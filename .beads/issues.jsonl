{"id":"tcp-6yk","title":"Upgrade agent-mail-monitor into full agent-mail plugin (v1)","description":"## Goal\nGrow the single-purpose agent-mail-monitor plugin into a full agent-mail plugin (monitors + skills + agent) mirroring the am CLI seam, so any agent in a plugin-enabled repo is zero-to-productive on Agent Mail. Value = onboarding + host monitors + encoded discipline ON TOP of the MCP tools, never re-wrapping them.\n\n## Non-goals\nRe-implementing am MCP tools as skills. Autonomous send/ack by an agent (v1 triage is read-and-recommend only). Re-implementing am itself — the Deno layer is a THIN wrapper (am is the capability CLI).\n\n## Stack (decided)\nDeno 2.x + strict TS + Commander + Zod + Agent-Skills SKILL.md; Deno.Command with argv (no shell); shipped as .ts via `deno run` (not compiled). Scope = \"wrapper + introspection\": doctor/capabilities/schema subcommands emit versioned JSON envelopes. Deferred blueprint pieces + escalation triggers: vault design/agent-mail-plugin-deferrals.md. Rationale source: raw/gpt-pi-skills-cli.md.\n\n## Success Criteria\nRename landed with marketplace renames map; scripts ported to the Deno stack; product multi-project watch mode; doctor catches the silent registration gap; capabilities/schema introspection; onboard/product-setup/coordinate skills (decision-procedure style, with routing evals); read-only mail-triage agent; plugin validates; deno fmt/lint/check + tests pass; installs migrate cleanly.","status":"closed","priority":1,"issue_type":"epic","created_at":"2026-07-24T15:49:21.814708850Z","created_by":"gulp","updated_at":"2026-07-24T18:02:54.269857393Z","closed_at":"2026-07-24T18:02:54.269653356Z","close_reason":"v1 shipped: read-only Agent Mail plugin landed on main (a8059bd, pushed) and live at 0.4.0/project/enabled. All 10 children closed — CLI (watch/product/monitor/doctor/capabilities/schema), product mode, mail-triage agent, and 5 skills (onboarding-to-agent-mail, wiring-a-product-bus, toggle, doctor, driving-swarms-with-mail). id kept agent-mail-monitor (displayName 'Agent Mail', no rename) per .1. Follow-ups tracked outside the epic: manual monitor re-arm deferred (no live identity this session); human-only rm of superseded scripts/{mail-monitor,watch-mail}.sh.","source_repo":".","compaction_level":0,"original_size":0}
{"id":"tcp-6yk.1","title":"Rebrand plugin surface to \"Agent Mail\" (displayName/description), keep id agent-mail-monitor","description":"## Goal\nPresent the plugin as the fuller **\"Agent Mail\"** product (monitors + skills +\nread-only triage agent) **without renaming the plugin id**. The id\n`agent-mail-monitor` is load-bearing for installs — a consumer's `enabledPlugins`\nkeys on `<id>@marketplace`, skill namespaces are `<id>:skill`, and the plugin\ncache dir is `<id>/<version>/` — so changing it silently disables the plugin for\nanyone who installed it. Only the **presentational** surface changes.\n\n## Non-goals\n- Do NOT change plugin.json `name` (the id) — that is the breakage this bead\n  deliberately avoids. Decision recorded 2026-07-24: consumers already installed\n  `agent-mail-monitor`; keep the id.\n- No `renames` map — unnecessary when the id is unchanged (it exists only to\n  migrate an id rename, which we are not doing).\n\n## Acceptance Criteria\n- marketplace.json entry: `displayName` \"Agent Mail Monitor\" -> \"Agent Mail\";\n  `description` broadened to cover monitors + skills + read-only triage agent\n  (not just the monitor).\n- plugin.json `description` broadened to match; `name` UNCHANGED\n  (`agent-mail-monitor`).\n- version bump plugin.json -> 0.4.0 (feature growth).\n- README / docs framing updated to the \"Agent Mail\" product where the reference\n  is presentational; the id `agent-mail-monitor` retained in skill namespaces,\n  cache paths, and script echo strings.\n- `claude plugin validate .` passes.\n\n## Verification\n- `claude plugin validate .` passes.\n- `jq -r .name plugins/agent-mail-monitor/.claude-plugin/plugin.json` == `agent-mail-monitor` (unchanged).\n- `jq -r '.plugins[0].displayName' .claude-plugin/marketplace.json` == `Agent Mail`.\n- The existing `enabledPlugins` key `agent-mail-monitor@tiny-claude-plugins`\n  still resolves — no id change, so no consumer re-enable needed.","status":"closed","priority":1,"issue_type":"feature","created_at":"2026-07-24T15:49:47.041054960Z","created_by":"gulp","updated_at":"2026-07-24T17:02:44.641637044Z","closed_at":"2026-07-24T17:02:44.641304623Z","close_reason":"Rebrand landed in 49cc0d8: displayName -> Agent Mail, broadened descriptions, version 0.4.0, id agent-mail-monitor unchanged (no renames map). claude plugin validate passed.","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"tcp-6yk.1","depends_on_id":"tcp-6yk","type":"parent-child","created_at":"2026-07-24T15:49:47.041054960Z","created_by":"gulp","metadata":"{}","thread_id":""}]}
{"id":"tcp-6yk.10","title":"Reconsider 'coordinate' skill: boundary leak + loaded verb vs Agent Mail scope","description":"## Goal\nDecide whether the `coordinate` skill (landed in tcp-6yk.6) belongs in this\nplugin at all, and if so under what name — before v1 ships (tcp-6yk.8).\n\n## Background\nThe plugin's identity is **Agent Mail**: monitor inboxes, query mail, triage\nmail. The `coordinate` skill instead teaches the *whole* shared-tree swarm loop\n(reserve -> announce -> work -> commit --only -> release -> close). Two concerns:\n\n1. **Domain/boundary leak.** Swarm coordination is a broader workflow than Agent\n   Mail. It *uses* AM's file-reservation + messaging primitives, but the skill\n   also covers git commit discipline, bead lifecycle, and build slots — none of\n   which are AM. A user installing an \"Agent Mail\" plugin does not expect a\n   general swarm-coordination playbook to ride along.\n2. **Loaded verb.** \"coordinate\" is generic and overloaded; it reads as a\n   top-level capability claim rather than \"the mail-driven slice of coordination.\"\n\n## Non-goals\n- Not re-litigating whether the swarm loop is *useful* (it is) — only whether it\n  belongs under the Agent Mail plugin's namespace/identity.\n- No behavior change to the watch/query/triage code.\n\n## Options to weigh\n- **Keep + rename** to something scoped to the mail seam (e.g. `mail-driven-swarm`\n  / `reserve-and-announce`) so the name doesn't overclaim.\n- **Narrow the skill** to only the AM-native primitives (reservations + threads),\n  cross-linking out to the broader swarm rules rather than restating git/beads.\n- **Relocate** the skill to a coordination-focused plugin/home and drop it here.\n- **Keep as-is** if we decide AM's reservation/messaging seam legitimately anchors\n  the whole loop for this audience.\n\n## Acceptance Criteria\n- A decision recorded (keep-as-is / rename / narrow / relocate) with a one-line\n  rationale, reconciled against the plugin's stated scope in plugin.json +\n  marketplace.json description.\n- If rename/narrow/relocate: the change applied (skill dir + any SKILL.md refs +\n  disable-model-invocation intact) and re-validated with skill-check.\n- v1 ships (tcp-6yk.8) consistent with the decision — no dangling \"coordinate\"\n  name if we chose to rename.\n\n## Verification\n- `skill-check check <skill-dir> --strict --no-security-scan` PASS after any edit.\n- plugin description and skill set read as coherent (no scope overclaim) on a\n  fresh read of marketplace.json + the skills/ listing.","status":"closed","priority":3,"issue_type":"task","created_at":"2026-07-24T17:18:55.452309090Z","created_by":"gulp","updated_at":"2026-07-24T17:39:43.400731770Z","closed_at":"2026-07-24T17:39:43.400326290Z","close_reason":"Renamed skills/coordinate → driving-swarms-with-mail (gerund form, matching the skill-naming convention); description re-anchored on the Agent Mail reservation/thread seam, git/tracker steps demoted to generic hygiene. skill-check --strict PASS (100/100).","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"tcp-6yk.10","depends_on_id":"tcp-6yk","type":"parent-child","created_at":"2026-07-24T17:18:55.452309090Z","created_by":"gulp","metadata":"{}","thread_id":""}]}
{"id":"tcp-6yk.2","title":"Add product (multi-project) mode to the Deno watch loop","description":"## Acceptance Criteria\nDeno/TS product-mode branch: AGENT_MAIL_PRODUCT env / --product flag; when set, poll `am products inbox <key> <agent> --since-ts <last created_ts iso> --json` via Deno.Command (argv); Zod-validate the {messages:[...]} envelope; watermark on created_ts (am returns messages[0]=newest); baseline adopts without replay; emit a human line labeled with project via project_id->slug map from `am list-projects` (also Zod-validated); --limit 200 with a no-silent-cap log if a full page returns.\n## Verification\ndeno test: fake `am products inbox` grows across polls; asserts no baseline replay + only-new emit + project label + Zod rejects a malformed payload loudly (no crash).","status":"closed","priority":1,"issue_type":"feature","created_at":"2026-07-24T15:49:47.106659686Z","created_by":"gulp","updated_at":"2026-07-24T17:18:25.883176537Z","closed_at":"2026-07-24T17:18:25.882938895Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"tcp-6yk.2","depends_on_id":"tcp-6yk","type":"parent-child","created_at":"2026-07-24T15:49:47.106659686Z","created_by":"gulp","metadata":"{}","thread_id":""},{"issue_id":"tcp-6yk.2","depends_on_id":"tcp-6yk.9","type":"blocks","created_at":"2026-07-24T16:25:09.577903111Z","created_by":"gulp","metadata":"{}","thread_id":""}]}
{"id":"tcp-6yk.3","title":"Doctor + capabilities/schema introspection (+ product registration-gap check)","description":"## Acceptance Criteria\ndoctor/capabilities/schema subcommands emit versioned JSON envelopes ({schemaVersion,ok,data|error,meta}); doctor gains a `deno` presence check. Product registration-gap check: when AGENT_MAIL_PRODUCT set, enumerate linked projects (`am products status <key> --json`), verify agent registered in each (`am agents list --project <p> --json`); FAIL naming any project missing the identity; resources/product-registration-gap.md fix guide; no-op when product unset.\n## Verification\ndeno test: doctor with a product where identity missing in one project -> FAIL naming it; `capabilities --json` and `schema doctor --json` validate against the envelope schema.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-07-24T15:49:47.176397380Z","created_by":"gulp","updated_at":"2026-07-24T17:28:05.729451806Z","closed_at":"2026-07-24T17:28:05.729218302Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"tcp-6yk.3","depends_on_id":"tcp-6yk","type":"parent-child","created_at":"2026-07-24T15:49:47.176397380Z","created_by":"gulp","metadata":"{}","thread_id":""},{"issue_id":"tcp-6yk.3","depends_on_id":"tcp-6yk.9","type":"blocks","created_at":"2026-07-24T16:25:09.625308185Z","created_by":"gulp","metadata":"{}","thread_id":""}]}
{"id":"tcp-6yk.4","title":"onboard skill: zero-to-productive first run","description":"## Acceptance Criteria\nskills/onboard SKILL.md + scripts: delegate to am setup (declare MCP) and am agent (cockpit); register durable identity (am agents register); point to toggle to arm the watch; idempotent + safe to re-run.\n## Verification\nSkill body references am setup/am agent, does not reinvent MCP declaration.","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-07-24T15:49:47.243522363Z","created_by":"gulp","updated_at":"2026-07-24T17:46:33.119493076Z","closed_at":"2026-07-24T17:46:33.119172356Z","close_reason":"Landed onboarding-to-agent-mail skill (fbae4cf): am setup → am agent cockpit → am agents register → cross-link toggle to arm watch; idempotent. skill-check PASS.","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"tcp-6yk.4","depends_on_id":"tcp-6yk","type":"parent-child","created_at":"2026-07-24T15:49:47.243522363Z","created_by":"gulp","metadata":"{}","thread_id":""},{"issue_id":"tcp-6yk.4","depends_on_id":"tcp-6yk.3","type":"blocks","created_at":"2026-07-24T16:25:09.679756245Z","created_by":"gulp","metadata":"{}","thread_id":""}]}
{"id":"tcp-6yk.5","title":"product-setup skill: wire N repos into one product bus","description":"## Acceptance Criteria\nskills/product-setup SKILL.md: guided am products ensure + link per repo + am agents register per repo (closes the silent-gap by construction); explains created_ts aggregation.\n## Verification\nSkill steps map 1:1 to the product wiring in design/agent-mail-inbox-topology.md.","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-07-24T15:49:47.310268627Z","created_by":"gulp","updated_at":"2026-07-24T17:46:33.401118015Z","closed_at":"2026-07-24T17:46:33.400862209Z","close_reason":"Landed wiring-a-product-bus skill (8e0483e): products ensure/link + agents register per repo (closes silent gap by construction) + created_ts aggregation; mapped 1:1 to design/agent-mail-inbox-topology.md. skill-check PASS.","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"tcp-6yk.5","depends_on_id":"tcp-6yk","type":"parent-child","created_at":"2026-07-24T15:49:47.310268627Z","created_by":"gulp","metadata":"{}","thread_id":""},{"issue_id":"tcp-6yk.5","depends_on_id":"tcp-6yk.2","type":"blocks","created_at":"2026-07-24T16:25:09.747915076Z","created_by":"gulp","metadata":"{}","thread_id":""}]}
{"id":"tcp-6yk.6","title":"coordinate skill: portable reserve->commit->release discipline","description":"## Acceptance Criteria\nskills/coordinate SKILL.md lifts the reserve->announce->work->commit --only->release->close loop out of vault CLAUDE.md, de-vault-ified so any swarm repo gets it; covers shared-tree gotchas (commit --only, review committed state).\n## Verification\nNo vault-specific paths/prefixes; reads as generic swarm contract.","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-07-24T15:49:47.372388998Z","created_by":"gulp","updated_at":"2026-07-24T17:13:24.790554674Z","closed_at":"2026-07-24T17:13:24.790357109Z","close_reason":"Landed plugins/agent-mail-monitor/skills/coordinate/SKILL.md (commit 0c5da10) — de-vault-ified reserve->announce->work->commit --only->release->close loop as a portable Agent-Skills SKILL.md. skill-check --strict --no-security-scan: PASS, 0 errors/0 warnings, 100/100.","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"tcp-6yk.6","depends_on_id":"tcp-6yk","type":"parent-child","created_at":"2026-07-24T15:49:47.372388998Z","created_by":"gulp","metadata":"{}","thread_id":""},{"issue_id":"tcp-6yk.6","depends_on_id":"tcp-6yk.9","type":"blocks","created_at":"2026-07-24T16:25:09.805225933Z","created_by":"gulp","metadata":"{}","thread_id":""}]}
{"id":"tcp-6yk.7","title":"mail-triage agent (read-and-recommend only)","description":"## Acceptance Criteria\nagents/mail-triage.md: reads (product) inbox, proposes routing/priorities; tools limited to read + am read surfaces; NEVER send/ack; frontmatter documents the read-only guardrail.\n## Verification\nAgent tool allowlist excludes send_message/reply_message/acknowledge_message.","status":"closed","priority":3,"issue_type":"feature","created_at":"2026-07-24T15:49:47.438161342Z","created_by":"gulp","updated_at":"2026-07-24T17:48:40.161529378Z","closed_at":"2026-07-24T17:48:40.161298790Z","close_reason":"Landed agents/mail-triage.md: read-only inbox triage advisor; tools allowlist excludes send/reply/ack + all mutators (read-and-recommend only).","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"tcp-6yk.7","depends_on_id":"tcp-6yk","type":"parent-child","created_at":"2026-07-24T15:49:47.438161342Z","created_by":"gulp","metadata":"{}","thread_id":""},{"issue_id":"tcp-6yk.7","depends_on_id":"tcp-6yk.4","type":"blocks","created_at":"2026-07-24T16:25:09.859576207Z","created_by":"gulp","metadata":"{}","thread_id":""},{"issue_id":"tcp-6yk.7","depends_on_id":"tcp-6yk.6","type":"blocks","created_at":"2026-07-24T16:25:09.913006564Z","created_by":"gulp","metadata":"{}","thread_id":""}]}
{"id":"tcp-6yk.8","title":"Tests + docs + validate + commit + push + re-arm","description":"## Acceptance Criteria\nExtend tests/ for product mode; update README + CLAUDE.md (skill inventory, product mode, agent).\n`claude plugin validate .` passes; commit -F, push.\nRe-point the vault install under the new name: verify the marketplace `renames` map migrates the install, else `claude plugin uninstall agent-mail-monitor` + `claude plugin install agent-mail@tiny-claude-plugins --scope project` (remote+project-scope is already PROVEN for 0.3.0 — this proves it survives the rename to 0.4.0).\nRewrite install docs to the proven remote project-scope commands (`claude plugin marketplace add gulp/tiny-claude-plugins --scope project` + `install agent-mail@... --scope project`) — no local-path/shim language.\nRe-arm PlumHare from the new 0.4.0 path.\n## Verification\nplugin validate green; tests pass; git push ok; `claude plugin list` shows agent-mail@tiny-claude-plugins 0.4.0 Scope: project enabled.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-07-24T15:49:47.504246411Z","created_by":"gulp","updated_at":"2026-07-24T18:02:27.973615560Z","closed_at":"2026-07-24T18:02:27.973379451Z","close_reason":"Landed as a8059bd on main (pushed; origin/main==main). RECONCILED vs stale AC: per tcp-6yk.1 the id STAYS agent-mail-monitor (displayName 'Agent Mail' only) — NO rename to agent-mail@, NO renames map, install docs stay on the proven remote project-scope commands. Gates green on landed tree: claude plugin validate . passed; deno check OK; deno test --allow-write = 3 passed (product gap + introspection + product watch). Live install refreshed 0.3.0->0.4.0 at project scope (/home/gulp/Vaults/everything) and enabled; claude plugin list shows agent-mail-monitor@tiny-claude-plugins 0.4.0 Scope: project enabled. DEFERRED: monitor re-arm — the 0.4.0 entrypoint is 'deno run src/cli.ts monitor' (not mail-monitor.sh), auto-arms next session under when:always; manual re-arm skipped because the intended identity 'PlumHare' is not registered in the vault project (whois: not found) and this session has no AGENT_NAME, so a watch now would be idle. Human-only cleanup of superseded scripts/{mail-monitor,watch-mail}.sh remains (dcg blocks rm under /home).","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"tcp-6yk.8","depends_on_id":"tcp-6yk","type":"parent-child","created_at":"2026-07-24T15:49:47.504246411Z","created_by":"gulp","metadata":"{}","thread_id":""},{"issue_id":"tcp-6yk.8","depends_on_id":"tcp-6yk.1","type":"blocks","created_at":"2026-07-24T16:44:40.204719932Z","created_by":"gulp","metadata":"{}","thread_id":""},{"issue_id":"tcp-6yk.8","depends_on_id":"tcp-6yk.10","type":"blocks","created_at":"2026-07-24T17:19:04.007304644Z","created_by":"gulp","metadata":"{}","thread_id":""},{"issue_id":"tcp-6yk.8","depends_on_id":"tcp-6yk.2","type":"blocks","created_at":"2026-07-24T16:25:09.967145568Z","created_by":"gulp","metadata":"{}","thread_id":""},{"issue_id":"tcp-6yk.8","depends_on_id":"tcp-6yk.3","type":"blocks","created_at":"2026-07-24T16:25:10.023708585Z","created_by":"gulp","metadata":"{}","thread_id":""},{"issue_id":"tcp-6yk.8","depends_on_id":"tcp-6yk.4","type":"blocks","created_at":"2026-07-24T16:25:10.078957696Z","created_by":"gulp","metadata":"{}","thread_id":""},{"issue_id":"tcp-6yk.8","depends_on_id":"tcp-6yk.5","type":"blocks","created_at":"2026-07-24T16:25:10.137845448Z","created_by":"gulp","metadata":"{}","thread_id":""},{"issue_id":"tcp-6yk.8","depends_on_id":"tcp-6yk.6","type":"blocks","created_at":"2026-07-24T16:25:10.193726189Z","created_by":"gulp","metadata":"{}","thread_id":""},{"issue_id":"tcp-6yk.8","depends_on_id":"tcp-6yk.7","type":"blocks","created_at":"2026-07-24T16:25:10.250503934Z","created_by":"gulp","metadata":"{}","thread_id":""}]}
{"id":"tcp-6yk.9","title":"Scaffold the Deno+Commander+Zod plugin foundation","description":"## Acceptance Criteria\nStand up the Deno foundation the other lanes build on: deno.json + committed deno.lock; src/cli.ts Commander skeleton (npm:commander) with global --json/--output/--cwd; a core layer (versioned envelope {schemaVersion,ok,data|error,meta}, exit-code map 0/2/3/4/5/6/70 via Commander exitOverride); an `am` adapter using Deno.Command with argv arrays (no sh -c); shebang entrypoints (`#!/usr/bin/env -S deno run --allow-run=am ...`) replacing the bash monitor/watch entrypoints; monitors.json points at the deno entrypoint. Watch loop keeps emitting HUMAN notification lines to stdout (NOT envelopes) — envelope is for query/introspection commands only. SIGTERM/SIGINT -> graceful exit.\n## Verification\ndeno check src/cli.ts clean; `cli.ts --help` and `cli.ts doctor --json` run; Monitor host execs the deno entrypoint and a fake am produces a MAIL line.","status":"closed","priority":1,"issue_type":"feature","created_at":"2026-07-24T16:14:46.260280971Z","created_by":"gulp","updated_at":"2026-07-24T17:02:03.522978935Z","closed_at":"2026-07-24T17:02:03.522774847Z","close_reason":"Deno foundation landed in 8344ade: deno.json+lock, cli.ts (Commander skeleton, global flags, graceful shutdown), core (envelope/exit/am adapter), watch loop port, monitors.json -> deno entrypoint. Verified: deno check clean, --help, doctor --json envelope, fake-am MAIL line.","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"tcp-6yk.9","depends_on_id":"tcp-6yk","type":"parent-child","created_at":"2026-07-24T16:14:46.260280971Z","created_by":"gulp","metadata":"{}","thread_id":""}]}
{"id":"tcp-ald","title":"Decide watch-mail.sh fate: port to FS backend or deprecate","description":"## Goal\n\nDecide the fate of the legacy bash watch path `scripts/watch-mail.sh`: port it to\nthe canonical git-mailbox FS backend, or deprecate it in favor of the Deno\n`watch`/`monitor` commands.\n\n## Background\n\nThe FS-backend epic (tcp-p0x.16) moved the Deno `watch`/`monitor` notification\npath off `am check-inbox` (which CONSUMES — routes through the daemon's\n`fetch_inbox`, marking mail read) onto a non-consuming read of the append-only\ncanonical git-mailbox (`snapshotMailbox`). Retirement of `pollInbox` landed in\ntcp-p0x.16.4 (`b700f64`).\n\n`scripts/watch-mail.sh` is the OLD bash entrypoint and STILL shells out to `am\ncheck-inbox` — so it still consumes. tcp-p0x.16.6 (`9c6dd25`) corrected its usage\ntext to stop falsely claiming \"read-only\" and to point users at the Deno path, but\nthe script itself was left behaviorally unchanged. The plan\n(`docs/plans/agent-mail-fs-backend.md` → \"## Not yet specified\") explicitly parked\nthe port-or-deprecate decision as \"Decide during T4\"; T4 landed without resolving\nit (correctly — it is a scope decision, not in-scope code).\n\n## Non-goals\n\n- Re-implementing the FS reader (already exists: `src/core/mailbox.ts`).\n- Any change to the Deno `watch`/`monitor` path (already migrated).\n\n## Acceptance Criteria\n\n- A decision is recorded: PORT (rewire `watch-mail.sh` to call the Deno\n  `snapshotMailbox` path / the `monitor` command) or DEPRECATE (mark it legacy,\n  stop shipping/arming it, point all callers at the Deno path).\n- If PORT: `watch-mail.sh` no longer shells out to `am check-inbox`; its notify\n  path is non-consuming; `tests/test-watch-mail.sh` still passes.\n- If DEPRECATE: the script carries a clear deprecation notice, nothing auto-arms\n  it, and every doc/skill reference routes to the Deno path.\n- No surface left claiming the bash path is read-only while it still consumes.\n\n## Verification\n\n- `git grep -n \"check-inbox\" plugins/agent-mail-monitor/scripts/watch-mail.sh`\n  reflects the chosen outcome (absent if PORT; present-but-clearly-legacy if\n  DEPRECATE).\n- `tests/test-watch-mail.sh` green; `claude plugin validate .` passes.\n\n## Notes\n\nSurfaced by CrimsonBear (W7) at the close of tcp-p0x.16.4/.16.6. Small, isolated,\nnon-blocking — the epic shipped without it. Sequence at leisure.","status":"closed","priority":3,"issue_type":"task","created_at":"2026-07-25T04:15:59.856470885Z","created_by":"gulp","updated_at":"2026-07-25T08:09:59.500852961Z","closed_at":"2026-07-25T08:09:59.500648202Z","close_reason":"DEPRECATE (not port): monitors.json already arms only the Deno src/cli.ts monitor entrypoint (FS-backed, non-consuming) and the toggle skill already points there (tcp-p0x.3) — nothing live referenced watch-mail.sh. Strengthened the script's DEPRECATED header/usage notice (still shells out to check-inbox, still consumes if invoked by hand — dcg blocks rm under /home so it can't be physically deleted). Recorded the decision in docs/plans/agent-mail-fs-backend.md. tests/test-watch-mail.sh unchanged and green (behavior unchanged). claude plugin validate . passes.","source_repo":".","compaction_level":0,"original_size":0}
{"id":"tcp-g1z","title":"UX: should MAIL_WATCH_FILTER be independent of MAIL_WATCH_MODE, or coupled to actionable?","description":"## Goal\nRevisit whether `MAIL_WATCH_FILTER` (ack|urgent) should stay **independent** of\n`MAIL_WATCH_MODE` (basic|actionable), or be coupled to actionable mode. Raised by\nFoggySnow during the tcp-p0x dogfood: today a filter suppresses non-matching mail\neven in `basic` mode (fires only on matching messages, just without the ack/urgency\nmarker). That is the INTENDED, tested behavior (locked by watch_test.ts under\ntcp-p0x.7) — not a defect. This bead is a UX question, not a bug: is decoupling the\nright ergonomics, or is \"filter implies actionable\" less surprising?\n\n## Non-goals\n- No behavior change committed by opening this bead. It captures the open question so\n  it is not lost; any change is decided here, then implemented under its own bead.\n\n## Acceptance Criteria\n- A decision is recorded: keep independent (with rationale) OR couple filter to\n  actionable mode (with a follow-up implementation bead + doc/test updates).\n\n## Verification\nClose comment states the decision and, if a change is chosen, links the implementation\nbead. README/doctor docs reflect whichever semantics stand.","status":"open","priority":4,"issue_type":"task","created_at":"2026-07-25T10:18:19.530374892Z","created_by":"gulp","updated_at":"2026-07-25T10:18:19.530374892Z","source_repo":".","compaction_level":0,"original_size":0,"labels":["agent-mail-monitor","ux"]}
{"id":"tcp-hc9","title":"upstream: am agents list/show reject a positional project key with no -p hint (am 0.3.21)","description":"## Goal\nReport upstream (Dicklesworthstone/mcp_agent_mail_rust, am 0.3.21): `am agents list`\nand `am agents show` reject a **positional** project key with a hard error and no hint\nto use `-p/--project`. This is an upstream `am` CLI ergonomics bug, an EXPLICIT non-goal\nof epic tcp-p0x (see its Non-goals + tcp-p0x.5's `## Non-goals / upstream`) — tracked\nhere so the upstream filing is not lost after the epic closes.\n\n## Non-goals\n- No plugin change. The plugin already invokes `am agents list -p <key>` correctly;\n  this is purely about the upstream CLI's positional-arg handling + error message.\n\n## Steps to Reproduce\n1. `am agents list <project-key>` (positional key, e.g. an absolute repo path).\n2. Observe: the CLI rejects it as an unexpected argument with no hint that the\n   supported form is `am agents list -p <project-key>`.\n3. Same for `am agents show <key>`.\nExpected: either accept the positional key, or fail with a hint naming `-p/--project`.\n\n## Acceptance Criteria\n- An upstream issue is filed against Dicklesworthstone/mcp_agent_mail_rust with the\n  repro above and the installed version (am 0.3.21); the issue link is recorded in a\n  close comment here.\n- OR, if triaged as won't-file, a one-line rationale is recorded and the bead closed.\n\n## Verification\nClose comment carries the upstream issue URL (or the won't-file rationale).","status":"open","priority":3,"issue_type":"bug","created_at":"2026-07-25T10:18:19.348115993Z","created_by":"gulp","updated_at":"2026-07-25T10:18:19.348115993Z","source_repo":".","compaction_level":0,"original_size":0,"labels":["agent-mail","upstream"]}
{"id":"tcp-lsp","title":"plan-to-beads: add reconcile mode for already-beadified plans (greenfield-assumption fix)","description":"## Goal\n\nAdd a **reconcile mode** to the `plan-to-beads` skill\n(`.claude/skills/plan-to-beads/SKILL.md`) so that running it against a plan whose\nbeads **already exist** updates the existing graph instead of duplicating it. The\nskill's current flow assumes greenfield: its execution step runs\n`author_beads.py --execute`, which is non-idempotent and *creates* beads, so a\nre-beadify (or a plan written from pre-existing beads) yields a duplicate graph.\n\nEvidence + design: `design/plan-to-beads-reconcile-mode.md` (this run against\nepic `tcp-6yk`, where all 10 beads pre-existed).\n\n## Non-goals\n\n- Making `author_beads.py --execute` itself idempotent (upsert-by-key) — larger\n  change; the detection-gate branch is the cheaper, sufficient fix for now.\n- Removing the greenfield path — keep `author_beads.py` for genuinely new graphs.\n\n## Acceptance Criteria\n\n- `SKILL.md` gains a **Step 0 / detection gate**: query the id prefix\n  (`br list --json | jq 'select(.id|startswith(\"<prefix>\"))'`) BEFORE the\n  author/dry-run steps; if beads for this plan already exist, branch to reconcile\n  mode rather than `--execute`.\n- Reconcile mode documents the three moves proven this run: (1) self-containment +\n  drift check via `br show` → `br update --title/--description`; (2) add the\n  missing `blocks` edges that gate the frontier (parent-child does NOT gate);\n  (3) verify with `br ready` + `bv --robot-triage --graph-root <epic>`.\n- The `bv` output-path gotcha is recorded in `references/br-bv-mechanics.md`:\n  v0.18.0 nests under a top-level `triage` key\n  (`.triage.project_health.graph.has_cycles`); a bare `.project_health.…` path\n  returns `null`, which must not be misread as \"no cycles\".\n\n## Verification\n\n- Re-running `plan-to-beads` on an already-beadified plan hits the detection gate\n  and does not create duplicate ids (`wc -l .beads/issues.jsonl` unchanged by a\n  reconcile pass except for edge/field updates).\n- Grep `SKILL.md` for the Step 0 gate and the reconcile-mode block.\n- `references/br-bv-mechanics.md` contains the `triage`-key path note.","status":"tombstone","priority":2,"issue_type":"feature","created_at":"2026-07-24T16:26:35.531060403Z","created_by":"gulp","updated_at":"2026-07-24T16:26:58.588944186Z","source_repo":".","deleted_at":"2026-07-24T16:26:58.588879995Z","deleted_by":"gulp","delete_reason":"delete","original_type":"feature","compaction_level":0,"original_size":0}
{"id":"tcp-p0x","title":"Agent-mail plugin follow-up: coordinator cross-project message lookup + v0.3.0 dogfood fixes","description":"## Goal\nFollow-up work on the `agent-mail-monitor` plugin (shipped v1 / 0.4.0 under epic tcp-6yk),\ndriven by a coordinator-usability insight and a dogfooding review from GrayBrook (an agent\nworking out of /home/gulp/Vaults/everything) captured in Agent Mail message #27588.\n\nTwo threads of work:\n1. A coordinator working across many repos / project-ids needs to resolve an Agent Mail\n   message by its (server-wide-unique) id in ONE command, without knowing which project it\n   lives in. Today `am robot message <id>` is project-scoped (falls back to CWD), so a\n   coordinator must guess the project — verified this turn: locating #27588 required trying\n   two projects by hand.\n2. GrayBrook's 7 session-boot findings, the big three verified against the shipped files.\n\n## Non-goals\n- No rename of the plugin id (stays `agent-mail-monitor`; displayName \"Agent Mail\").\n- No write/mutation surface added — the plugin stays read-only by construction.\n- Upstream `am` CLI bugs (e.g. `am agents list` rejecting a positional project key) are\n  noted for reporting upstream, not fixed here.\n\n## Success Criteria\n- `agent-mail message <global-id>` resolves a message across projects in one command (F1).\n- Doctor's mcp-declaration remedy handles the rejected-server state (F2).\n- The toggle skill arms the CORRECT 0.4.0 entrypoint and documents a mid-session identity\n  route (F3).\n- Doctor/onboarding surface existing registered identities when AGENT_NAME is unset (F4).\n- Polish items triaged (F5).\n- All gates green (`claude plugin validate .`, `deno check`, `deno test --allow-write`,\n  `skill-check` on every touched skill), version bumped, landed on main.","status":"closed","priority":1,"issue_type":"epic","created_at":"2026-07-24T18:16:06.660911207Z","created_by":"gulp","updated_at":"2026-07-25T10:18:30.846253539Z","closed_at":"2026-07-25T10:18:30.846040294Z","close_reason":"All 5 success criteria met (F1 tcp-p0x.1 cross-project message lookup; F2 tcp-p0x.2 doctor mcp four-state; F3 tcp-p0x.3 toggle entrypoint+identity; F4 tcp-p0x.4 identity-list; F5 tcp-p0x.5 polish). All 20+ children closed; gates green this session (deno fmt/lint/check/test, claude plugin validate); version 0.4.0 on main incl. review-fix df28f54. Two non-blocking threads spun out as tracking beads: tcp-hc9 (upstream am agents list positional-key bug — explicit non-goal, file upstream) and tcp-g1z (UX: MAIL_WATCH_FILTER/MODE independence — intended+tested, revisit ergonomics).","source_repo":".","compaction_level":0,"original_size":0}
{"id":"tcp-p0x.1","title":"agent-mail message <global-id> — cross-project message lookup in one command","description":"## Goal\nAdd a coordinator-facing, read-only `agent-mail message <global-id>` command that resolves\nan Agent Mail message by its server-wide-unique id across projects — in ONE command, with no\nproject guessing. This is the headline of the epic: it addresses the exact friction a\ncross-repo coordinator hits.\n\n## Background\nAgent Mail message ids are globally unique, but the read path is project-scoped:\n`am robot message <ID>` requires `--project` (falls back to CWD) and returns\n\"message #<id> not found\" when pointed at the wrong project. `am robot overview` is\ncross-project but returns only per-project counts, not a message. So there is no single `am`\ncommand mapping a bare id to its message. Verified this turn: finding #27588 required trying\n`tiny-claude-plugins` (not found) then the vault (found).\n\n## Design sketch\n- New subcommand in the Deno CLI: `agent-mail message <id> [--product <key>]`.\n- Resolution strategy: enumerate candidate projects via `am robot projects --json` (or, when\n  `--product`/$AGENT_MAIL_PRODUCT is set, the product-linked projects only), then probe\n  `am robot message <id> --project <p>` until one resolves; return it (project + message).\n- Read-only; emits the same versioned JSON envelope as the other query commands\n  (`--json`), plus a human view.\n- Bound the fan-out (log if projects are skipped); short-circuit on first hit.\n\n## Constraints\n- Read-only by construction — no mutation tools.\n- Reuse the existing envelope/exit-code contract (introspect.ts / envelope.ts).\n- Prefer a real `am` global primitive if one is added upstream; until then, wrap the fan-out.\n\n## Acceptance Criteria\n- `agent-mail message 27588` (no project flag) returns the message + its project in one\n  invocation from any cwd.\n- `--product <key>` scopes the search to product-linked projects.\n- Not-found across all candidates yields a clear, single diagnostic (not N stacked errors)\n  and a non-zero exit per the contract.\n- `--json` emits a valid versioned envelope; a test covers hit + miss.\n\n## Verification\n`deno test --allow-write` covers a resolved id and an unresolved id; `deno check` and\n`skill-check` (if a skill references it) stay green.","status":"closed","priority":1,"issue_type":"feature","created_at":"2026-07-24T18:18:13.338154723Z","created_by":"gulp","updated_at":"2026-07-25T05:19:46.342754536Z","closed_at":"2026-07-25T05:19:46.342557973Z","close_reason":"Implemented in commit 8182449: 'agent-mail message <id>' resolves a globally-unique id across projects (pure resolver + am-backed orchestrator, injectable deps, versioned envelope, honest NOT_FOUND/SERVER_UNREACHABLE contract, capped fan-out). Verified against live am (4519 resolves cross-project; bogus id -> one clean not-found + exit 1).","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"tcp-p0x.1","depends_on_id":"tcp-p0x","type":"parent-child","created_at":"2026-07-24T18:18:13.338154723Z","created_by":"gulp","metadata":"{}","thread_id":""}]}
{"id":"tcp-p0x.10","title":"diagnose-agent-mail-service skill: recognize server-wedge signature + correct probe ordering","description":"## Goal\nA `diagnose-agent-mail-service` skill (agent-mail-monitor plugin) that correctly diagnoses a\nsuspected Agent Mail **server wedge** — HTTP unresponsive while the process is still running —\nusing the right tool ordering and routing to recovery, without misreading the signature.\n\n## Background\n2026-07-24 incident (HazyRiver + WindyBarn): `am status` hung (10s timeout) while `:8765`\nstayed bound. A first snapshot misdiagnosed this as *\"unit inactive, orphan process holds the\nport.\"* Ground truth: the systemd unit was `active (running)` and the port-holder **was** its\nMain PID — a **wedged-but-alive** server, not a dead unit. The misdiagnosis came from probing\nwith the wrong tools.\n\nVerified tool split (this is the skill's core knowledge):\n\n| Command | Path | When server is wedged |\n|---|---|---|\n| `am status` | **HTTP** → server | **hangs** — it IS the symptom, never use it as the probe |\n| `am robot status` / `robot inbox` | **local sqlite** | return **instantly** — safe reads |\n| `am robot atc` | local sqlite | on this build returns `\"enabled\": false` (`AM_ATC_ENABLED=false`) — **not** a health surface |\n| `systemctl --user status mcp-agent-mail.service` | systemd | **authoritative**: managed? alive? Main PID? — beats `ss`/`pgrep`, which cannot tell managed-vs-orphan |\n\nRoot cause of the wedge was environmental: connection leak (`drop_close` on storage.sqlite3) +\nmemory/swap pressure + a **corrupt archive git repo** (`~/.mcp_agent_mail_git_mailbox_repo`,\n`git fsck` = empty objects) failing maintenance every tick.\n\n## Non-goals\n- Not an `am` source change; the CLI already has the pieces — the skill ties them into a playbook.\n- Does not auto-restart or auto-repair; it diagnoses and recommends (restart / archive-repair are\n  mutations the operator confirms).\n\n## Acceptance Criteria\n- Skill recognizes the **\"port bound + `am status` hangs\"** signature and names it a wedge, not\n  a dead unit.\n- Probe ordering is **systemctl-first → `am robot status` (local read) → HTTP last**, and it\n  explicitly warns NOT to diagnose with `am status` (the hanging call).\n- It names the recovery: `systemctl --user restart mcp-agent-mail.service` (clean because the\n  port-holder is the Main PID → no orphan/EADDRINUSE), and flags corrupt-archive as a **separate,\n  human-run** repair (empty-object cleanup is `find -delete`/`rm` under `/home`).\n- `skill-check check <dir> --strict --no-security-scan` → 0 errors.\n\n## Verification\nRun the skill against the currently-healthy server → it reports healthy via the local-DB read and\na fast HTTP probe. The documented recipe reproduces the 2026-07-24 incident's correct diagnosis\n(wedged-alive, restart clears it, archive repair separate).\n\n## Notes\nPairs with the systemd health-watchdog bead (the durable \"ensure service\" companion). The skill\nis the human-in-the-loop diagnosis; the watchdog is the unattended auto-recovery.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-07-24T19:16:11.945967621Z","created_by":"gulp","updated_at":"2026-07-24T19:29:52.608398836Z","closed_at":"2026-07-24T19:29:52.608186312Z","close_reason":"Built diagnose-agent-mail-service skill (SKILL.md + scripts/diagnose.sh + resources/recovery.md). skill-check 100/100, 0 errors. Happy-path + WEDGED/agent-skip/usage negative tests pass. NOTE: live re-probing corrected two details from this bead's original sketch — the wedge-safe local read is 'am robot inbox' (~0.15s), NOT 'robot status' (which aliases the slow 1.3-15.8s 'am status' synthesis and would false-positive a wedge); the decisive fast probe is curl to /mcp/ (any code=alive, 000=wedged).","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"tcp-p0x.10","depends_on_id":"tcp-p0x","type":"parent-child","created_at":"2026-07-24T19:16:11.945967621Z","created_by":"gulp","metadata":"{}","thread_id":""}]}
{"id":"tcp-p0x.11","title":"systemd health-watchdog for mcp-agent-mail: liveness-based restart (Restart=on-failure can't catch a wedged-alive process)","description":"## Goal\nMake the `mcp-agent-mail` systemd user service self-recover from a **wedged-but-alive** state\n(HTTP unresponsive while the process keeps running) — the failure mode the current\n`Restart=on-failure` cannot catch — plus add a memory guardrail.\n\n## Background\nThe unit (`~/.config/systemd/user/mcp-agent-mail.service`) already has `Restart=on-failure` +\n`RestartSec=3`. But a hung-alive process never exits or signals failure, so systemd sees it as\nhealthy and **never restarts it** — which is exactly why the 2026-07-24 wedge persisted for days\n(uptime since 2026-07-17). `Restart=on-failure` is liveness-blind; we need a **health**-based\nrestart. Memory pressure was also present (peak 826M, swap 292M), and an unclean write under that\npressure is the likely origin of the corrupt archive git repo.\n\n## Design sketch (confirm the two open questions first)\n- **Open Q1:** does `mcp-agent-mail serve` expose an HTTP health endpoint (e.g. `/health` on\n  127.0.0.1:8765)? **Open Q2:** does the binary `sd_notify(WATCHDOG=1)`?\n- **If Q2 yes →** native `WatchdogSec=` in the unit (simplest, in-process liveness).\n- **If Q2 no (build-agnostic, default plan) →** a `mcp-agent-mail-health.timer` + oneshot\n  `mcp-agent-mail-health.service` that probes the **HTTP path** — `timeout 6 am status`, or\n  `curl -fsS --max-time 5 http://127.0.0.1:8765/health` if the endpoint exists — and runs\n  `systemctl --user restart mcp-agent-mail.service` after N consecutive failures.\n  - ⚠ The probe MUST hit HTTP. `am robot status` reads local sqlite and returns fine *through*\n    a wedge, so it would hide the very failure we're detecting.\n- **Memory guardrail:** `MemoryHigh=`/`MemoryMax=` + `OOMPolicy=` so runaway memory forces a\n  clean restart instead of silent degradation (and stops the unclean-write → corrupt-archive cycle).\n- **Archive hygiene:** one-time repair of `~/.mcp_agent_mail_git_mailbox_repo` (remove empty\n  objects, `git fsck`, re-init if needed); optionally an `ExecStartPre=` fsck guard.\n\n## Non-goals\n- No change to the app binary; this is unit/timer + ops config only.\n- Not removing `Restart=on-failure` — it stays for the crash case; the watchdog adds the\n  wedge/liveness case it can't cover.\n\n## Acceptance Criteria\n- A wedged-but-alive server (HTTP unresponsive) is auto-restarted within a bounded window.\n- A healthy server is never needlessly restarted (probe hits HTTP, tolerates N transient misses).\n- A memory cap is in place with a defined OOM policy.\n- The two open questions (health endpoint? sd_notify?) are answered and the chosen mechanism\n  (native watchdog vs timer-probe) is recorded.\n\n## Verification\nInduce or await a wedge → the watchdog restarts the service within the probe interval;\n`systemctl --user show mcp-agent-mail.service -p NRestarts` increments and `am status` (HTTP)\nresponds again without manual intervention. Under a synthetic memory balloon, the cgroup cap\ntrips the configured OOM policy rather than swapping indefinitely.\n\n## Notes\nCompanion to the `diagnose-agent-mail-service` skill bead (human-in-loop diagnosis ↔ unattended\nauto-recovery). This is machine infra (systemd user units), not plugin code — tracked here with\nthe diagnosis skill so the pair stays discoverable; may re-home if a better tracker fits.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-07-24T19:16:12.090116066Z","created_by":"gulp","updated_at":"2026-07-24T19:35:03.448825094Z","closed_at":"2026-07-24T19:35:03.448486371Z","close_reason":"ELEGANT DECISION (2026-07-24): do NOT build a bespoke liveness-watchdog (health.timer + probe service). Live investigation showed the server already SELF-HEALS on restart — its startup integrity probe detected archive-ahead-of-DB + sqlite corruption and auto-recovered (recovered ~19 projects/6 messages from the archive; git fsck now exits 0; archive maintenance completes clean). The reason Restart=on-failure 'could not catch the wedge' is that a memory-bloated-but-alive process never EXITS. Fix: applied MemoryHigh=1200M/MemoryMax=1536M live+persistent via 'systemctl --user set-property' (drop-ins under ~/.config/systemd/user.control/). Runaway memory -> hard cap -> OOM-kill -> clean exit -> existing Restart=on-failure -> startup auto-recovery. One property turns the uncatchable wedge into an already-handled crash; zero new infra. Detection side is covered by the diagnose-agent-mail-service skill (tcp-p0x.10). If a CONFIRMED curl-000 wedge (not slow am status) ever recurs without a memory cause, reopen for a health.timer then — YAGNI until then. ALSO resolves the manual archive-repair follow-up: already self-healed by the 22:18 restart (fsck exit 0; the 1 remaining empty object is unreferenced/harmless).","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"tcp-p0x.11","depends_on_id":"tcp-p0x","type":"parent-child","created_at":"2026-07-24T19:16:12.090116066Z","created_by":"gulp","metadata":"{}","thread_id":""}]}
{"id":"tcp-p0x.12","title":"monitor: single-project watch recovery silently drops messages read-elsewhere during a poll outage (transport hole)","description":"## Goal\nMake the single-project mail watch's recovery lossless: a message that arrives\nduring a `check-inbox` poll outage must still produce a notification even if it is\nmarked read (by another session's fetch/ack) before the watch recovers.\n\n## Background\nThe watch keeps an id high-water mark (`last`) across failed polls and, on\nrecovery, re-emits messages from the latest `check-inbox` payload whose id exceeds\n`last`. But `am check-inbox` returns **unread messages only**. So a message that\n(a) arrives while polling is failing, then (b) is read elsewhere before the watch\nrecovers, has already left the unread set — recovery cannot see it, and it is\nsilently dropped. No error, no notification.\n\nThis is the root cause that compounded the 27635 miss: the monitor snapshot showed\n`check-inbox` failing 3× (\"did not return JSON\") then \"recovered\", but recovery\nover an unread-only source cannot replay what left the set during the outage. It is\ndistinct from the scope gap (watching only one project) tracked separately — this\nbug bites even when the message lands in the correctly-armed project.\n\nThe product-scoped watch does NOT have this hole: it uses `am products inbox\n--since-ts <ts>`, a server-side since-fetch that returns messages regardless of\nread state. The fix here brings single-project mode up to the same recovery\nguarantee (or routes single-project recovery through a since-timestamped read).\n\n## Non-goals\n- No change to the product-scoped watch (already lossless via `--since-ts`).\n- No new mutation surface — the watch stays read-only (never marks messages read).\n- Not the cross-project scope fix (separate work); this is recovery correctness\n  within one project.\n\n## Steps to Reproduce\n1. Arm the single-project watch for identity A in project P.\n2. Induce a poll outage (e.g. server wedge / `check-inbox` returning non-JSON for\n   several ticks).\n3. During the outage, send A a message in P, then have another session fetch/ack\n   it (marking it read) before the watch recovers.\n4. On recovery the watch emits nothing for that message — it is gone from the\n   unread set. Silent loss.\n\n## Acceptance Criteria\n- After a poll outage, a message that arrived during the outage and was read\n  elsewhere still produces exactly one notification on recovery (no silent drop).\n- The recovery path reads by a since-watermark that is independent of read state\n  (e.g. a `--since-ts`-style fetch), not the unread-only `check-inbox` set.\n- No duplicate notifications for messages already emitted before the outage.\n- Still read-only: recovery never marks a message read.\n- A test reproduces \"arrived-during-outage, read-elsewhere, still notified\".\n\n## Verification\n`deno test --allow-write` covers the outage-then-read-elsewhere recovery path;\n`deno check` stays green. Manually reproduce the four-step scenario and confirm one\nnotification lands on recovery.","status":"closed","priority":1,"issue_type":"bug","created_at":"2026-07-24T20:10:06.663383748Z","created_by":"gulp","updated_at":"2026-07-25T05:19:46.254116754Z","closed_at":"2026-07-25T05:19:46.253903910Z","close_reason":"Root cause structurally dissolved by tcp-p0x.16 (FS-backend swap): the notification path no longer reads check-inbox's unread-only source, so a message that leaves the unread set during an outage can no longer be dropped — it is read from the on-disk git-mailbox, which persists regardless of read/ack. Landed a regression test (commit cdc9874) that injects a throwing snapshot to reproduce the outage and asserts exactly-one lossless emission on recovery, locking the property in.","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"tcp-p0x.12","depends_on_id":"tcp-p0x","type":"parent-child","created_at":"2026-07-24T20:10:06.663383748Z","created_by":"gulp","metadata":"{}","thread_id":""},{"issue_id":"tcp-p0x.12","depends_on_id":"tcp-p0x.14","type":"blocks","created_at":"2026-07-25T00:24:59.725064072Z","created_by":"gulp","metadata":"{}","thread_id":""},{"issue_id":"tcp-p0x.12","depends_on_id":"tcp-p0x.16","type":"related","created_at":"2026-07-25T03:15:43.910478374Z","created_by":"WindyBarn","metadata":"{}","thread_id":""},{"issue_id":"tcp-p0x.12","depends_on_id":"tcp-p0x.6","type":"related","created_at":"2026-07-24T20:10:17.210232126Z","created_by":"gulp","metadata":"{}","thread_id":""}]}
{"id":"tcp-p0x.13","title":"product-scoped watch: group repos by workload not human + work-descriptive product-key naming (privacy)","description":"## Goal\nEstablish the convention for grouping an operator's repos into an Agent Mail\nproduct for the product-scoped watch: group by **workload (repos)**, never by\n**human**, and give the product a **work-descriptive** key. This keeps the\ncross-project subscription privacy-clean and prevents re-introducing operator\nidentity-exposure through the grouping.\n\n## Background\nThe product-scoped watch (sibling work) needs a product that links the operator's\nactive project-keys. The tempting shortcut — a durable record mapping one human to\ntheir N agent identities (\"operator roster\") — is exactly the identity-exposure\nmove flagged in this vault: it materializes, in durable state, that N agent\nidentities are all one person.\n\nPrior art (SPIFFE, W3C did:wba, Microsoft Entra) converges on: identity is\nworkload-scoped (SPIFFE trust domain ≈ AM `project_key`), and the human stays\n*above* the identity graph, never a node in it (did:wba: the human never becomes\nthe DID). An AM product groups **repos**, not people, so it is the privacy-safe\nprimitive — provided its key names the *work*, not the operator.\n\nThe only leak vector is the product key's name: `gulp-repos` / an email / a handle\nre-imports the exposure; `everything-ecosystem` (or similar work descriptor) stays\nworkload-scoped. This mirrors the discipline AM already enforces on agent names\n(identity/role-descriptive names rejected in favour of a random adjective+noun).\n\n## Non-goals\n- No `roster.jsonl` / operator entity — explicitly do NOT build it (overbuilt and\n  an exposure surface). If a multi-crew gap survives identity convergence, escalate\n  toward AM's own product / `am service` primitives, not a hand-rolled file.\n- No change to how agent identities are minted (that is the identity-convergence\n  bead).\n\n## Acceptance Criteria\n- A documented rule (in the plugin's product-watch docs / onboarding) that a\n  product key MUST be work-descriptive and MUST NOT encode the operator (no\n  human name, email, or handle).\n- The product-watch setup guidance groups by project-key (workload), and nowhere\n  records a human→identities mapping.\n- The rationale cites the workload-scoping principle (why grouping by repos, not by\n  human, is both sufficient and privacy-preserving).\n\n## Verification\nReview the product-watch setup docs: the naming rule is stated, the example key is\nwork-descriptive, and no artifact links agent identities to a human. A reviewer can\npoint to the rule and the SPIFFE/did:wba rationale.","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-07-24T20:10:06.811628983Z","created_by":"gulp","updated_at":"2026-07-25T08:07:57.140027879Z","closed_at":"2026-07-25T08:07:57.139810716Z","close_reason":"product-key naming guidance added to wiring-a-product-bus SKILL.md (SPIFFE/did:wba precedent, no-roster non-goal enforced)","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"tcp-p0x.13","depends_on_id":"tcp-p0x","type":"parent-child","created_at":"2026-07-24T20:10:06.811628983Z","created_by":"gulp","metadata":"{}","thread_id":""},{"issue_id":"tcp-p0x.13","depends_on_id":"tcp-p0x.6","type":"related","created_at":"2026-07-24T20:10:17.108271483Z","created_by":"gulp","metadata":"{}","thread_id":""}]}
{"id":"tcp-p0x.14","title":"monitor: swap watch backend to append-only canonical git-mailbox (retire check-inbox from the notification path)","description":"## Goal\n\nReplace the mail watch's notification backend: read new mail from the **append-only\ncanonical git-mailbox on disk** (already implemented as the shadow's `snapshotMailbox`\nin `src/core/mailbox.ts`), watermark by message id, and **remove `am check-inbox`\nfrom the notification path**. This is a genuinely read-only backend and it dissolves\nthree separately-filed failure modes at once.\n\n## Background\n\n**Verdict (source-verified against am v0.3.21):** `am check-inbox` is **not\nread-only when the am daemon is up (the default)**. Without `--direct` it\nunconditionally takes the daemon/HTTP path\n(`check_inbox_should_use_daemon(direct=false, *) → true`, lib.rs:7250 + tests at\nlib.rs:37108) and invokes the server's **consuming `fetch_inbox`** MCP tool, whose\ncontract is literally \"Retrieve recent messages … and mark returned messages read\"\n(messaging.rs:3106 → `apply_auto_read_timestamp`, :3358). The genuinely read-only\nSELECT path (`check_inbox_direct` → `fetch_inbox_metadata_rows_from_conn`,\nlib.rs:71126) is reachable ONLY with `--direct` AND no daemon listening. The\nplugin's `pollInbox` passes no `--direct`, and a daemon is normally live — so\n**every poll consumes**, marking mail read and stealing it from a later interactive\n`fetch_inbox(unread_only)`.\n\n**One root cause, three symptoms.** The watch trusts mutable/consumable SQLite\nread-state (via check-inbox) as source of truth instead of the append-only\ncanonical git-mailbox:\n1. **Consuming** — this verdict (the poll marks mail read).\n2. **DB-desync blind spot** — when SQLite under-represents the canonical archive,\n   `check-inbox` returns `rows_returned:0` while real mail sits on disk → false\n   silence (see the desync incident, msg 27647).\n3. **Transport hole** — `tcp-p0x.12`: unread-only recovery loses a message marked\n   read elsewhere during a poll outage.\n\nAll three vanish if the watch reads the canonical store and watermarks by id.\n\n**The backend already exists and is dogfooded.** `src/core/mailbox.ts`\n(`snapshotMailbox`) reads\n`<ROOT>/projects/<slug>/agents/<Agent>/inbox/YYYY/MM/<ts>__<subject>__<id>.md`,\nparses the id off the filename tail (anchored regex; regression-tested in\n`src/core/mailbox_test.ts` incl. the msg-27650 `___<id>` case), and is genuinely\nread-only at the OS level. The shadow command (`src/commands/shadow.ts`, part A)\nalready emits `MAIL #<id>` off this store and fired correctly live (msgs\n27657/27660/27661). This bead promotes part A from prototype to THE watch backend.\n\nDesign writeup (vault, for context — the bead is self-contained without it):\n`design/agent-mail-watch-canonical-backend.md` in the everything vault.\n\n## Non-goals\n\n- NOT wiring the optional doctor cross-check (shadow part B) — that is a follow-up.\n  A candidate non-consuming SQLite read (`am robot inbox`) must be source-verified\n  read-only first; do not assume it (a \"read-only\" label is exactly what proved\n  false for check-inbox).\n- NOT the cross-project scope work (`tcp-p0x.6`) — orthogonal; the FS reader\n  already supports multiple slugs, so this composes with it later.\n- NOT removing `pollInbox`/check-inbox from the codebase — the shadow experiment\n  still uses it as the divergence probe; only remove it from the *notification*\n  path (`watch` / `monitor`).\n\n## Acceptance Criteria\n\n- `runWatch` (single-project `watch` + the `monitor` entrypoint) emits one line per\n  NEW canonical message using `snapshotMailbox` + an id high-water mark; it no\n  longer calls `pollInbox`/`am check-inbox`. Arming the watch does not mark any mail\n  read (verify: `read_ts` unchanged after N polls over delivered mail).\n- Baseline poll adopts the current canonical id set silently (no replay of existing\n  mail); only post-launch arrivals emit. `--since <id>` still replays ids > N once.\n- Missing mailbox root / unparseable files degrade loudly (a warning line), never a\n  crash and never silent — reuse the shadow's `warnMissingRoot` / skipped-file\n  handling.\n- The private-layout coupling is guarded: a `doctor` check resolves the mailbox\n  root and asserts the `<ts>__<subject>__<id>.md` layout parses (fail loud if the\n  private layout shifts). Anchored parser + regression test already landed.\n- `tcp-p0x.12` (transport hole) is closed as subsumed: the FS backend is\n  append-only, so unread-only recovery loss cannot occur. Cross-link in the close.\n- Docs/comments already retracted (monitors.json, watch-mail.sh, toggle SKILL.md,\n  mail-triage.md, am.ts/mailbox.ts/shadow.ts/cli.ts) are consistent with the new\n  backend.\n\n## Verification\n\n- `deno test src/core/mailbox_test.ts` green (parser + snapshot).\n- New/updated watch test: given a temp mailbox tree, arming emits only\n  post-baseline ids, watermarks monotonically, and never invokes `am`.\n- Live dogfood: arm the watch as WindyBarn against the real git-mailbox root,\n  send a test message, confirm the notification fires AND `am check-inbox --direct`\n  (no daemon) / a direct SQLite read shows the message still UNREAD after the\n  notification.\n\n## Convergence gate (future, not this bead)\n\nWhen upstream enables the Product Bus, swap the FS reader for `am products inbox\n--since-ts` (a committed, non-consuming server API) and keep the FS reader as the\ndaemon-agnostic/offline fallback. Tracked by the vault note\n`wiki/agent-mail-product-bus-worktrees-gate.md`. Caution: `am products inbox`\nsilently bypasses `WORKTREES_ENABLED` offline, so confirm the bus is enabled\nserver-side before trusting the swap.","status":"closed","priority":1,"issue_type":"feature","created_at":"2026-07-25T00:24:47.571836024Z","created_by":"gulp","updated_at":"2026-07-25T03:15:58.464887314Z","closed_at":"2026-07-25T03:15:58.464685471Z","close_reason":"Superseded by epic tcp-p0x.16 (unified canonical git-mailbox watch backend). The FS backend swap is now tcp-p0x.16.1 (promote snapshotMailbox) + tcp-p0x.16.4 (retire check-inbox), with a prototype already dogfooded.","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"tcp-p0x.14","depends_on_id":"tcp-p0x","type":"parent-child","created_at":"2026-07-25T00:24:47.571836024Z","created_by":"gulp","metadata":"{}","thread_id":""}]}
{"id":"tcp-p0x.15","title":"monitor: show project-key (bus identity) in the notification label, not just repo basename","description":"## Goal\n\nSurface the **Agent Mail project-key** (the `human_key` = absolute cwd path the\nidentity is registered under) in the monitor's notification label, so a human\nwatching a multi-session/multi-repo setup can tell **which bus** a notification\ncame from at a glance.\n\nToday the notification renders as:\n\n    ● Monitor event: \"new Agent Mail for WindyBarn @ tiny-claude-plugins (0.4.0)\"\n\n`tiny-claude-plugins` there is the repo **basename**, not the project-key. Agent\nMail's project identity IS the absolute cwd path (a sibling repo and the vault are\ndifferent projects that can share a basename-ish label), so the basename alone\ndoes not answer \"which bus am I subscribed to\". Show the project-key.\n\n## Non-goals\n\n- No change to the per-message stdout line (`MAIL #<id> from <sender> …` from\n  `formatLine` in `src/commands/watch.ts`) — that is the delivery payload and\n  stays terse.\n- No new watch/transport behavior — this is a labeling/QoL change only.\n- Not the cross-project *watch* feature (tcp-p0x.6) nor global message *lookup*\n  (tcp-p0x.1) — this is purely how the single label identifies its bus.\n\n## Context / investigation (2026-07-25, WindyBarn)\n\n- The per-message notification body is `formatLine()` (`src/commands/watch.ts:27`)\n  → `MAIL #<id> from <from> [<importance>]: <subject>` — it carries no project id.\n- The `\"new Agent Mail for <AGENT_NAME> @ <project> (<version>)\"` text is the\n  **monitor-event label**, not emitted by `formatLine`; grep finds it nowhere in\n  the plugin source, so it is composed by the harness/monitor framing from the\n  monitor `name`/`description` (`monitors/monitors.json`) + resolved env\n  (`AGENT_NAME`, `CLAUDE_PROJECT_DIR`) + plugin version. Confirm the exact\n  emission site during implementation — it may require the plugin to emit its own\n  richer startup/label line rather than relying on harness composition.\n\n## Acceptance Criteria\n\n- The monitor notification identifies its bus by **project-key** (the absolute\n  path used as the Agent Mail `human_key`), not only the repo basename — e.g.\n  `… @ /home/gulp/projects/tiny-claude-plugins` (full path, or basename + full\n  path), so two buses that would collide on basename are distinguishable.\n- In **product / multi-project** mode the label reflects the product/bus scope\n  being watched, consistent with the single-project format.\n- The `AGENT_NAME`-unset idle path and the first-poll-FAILED path keep working\n  (labels there already name agent + project — keep them consistent).\n- No change to `formatLine`'s per-message output.\n\n## Steps to Reproduce\n\n1. Launch a session with `AGENT_NAME=WindyBarn` in a repo, monitor auto-arms.\n2. Receive a new Agent Mail message.\n3. Observe `● Monitor event: \"new Agent Mail for WindyBarn @ tiny-claude-plugins\n   (0.4.0)\"` — the `@` segment is the basename, so with a second bus of similar\n   basename you cannot tell which subscription fired.\n\n## Verification\n\n- Trigger a notification and confirm the label contains the full project-key.\n- If product mode is exercised, confirm its label names the product/bus scope.\n- `deno fmt`, `deno lint`, `deno check`, and `deno test` green in\n  `plugins/agent-mail-monitor/`.","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-07-25T01:47:03.469830871Z","created_by":"gulp","updated_at":"2026-07-25T08:12:50.851461344Z","closed_at":"2026-07-25T08:12:50.851261173Z","close_reason":"Fixed: single-project mode's noteResolvedBus() already printed the full project-key path on stdout; product mode had no equivalent bus-identity line at all. Added noteResolvedProductBus() (same pure-function+emit pattern), wired into both the monitor entrypoint (stdout) and standalone product command (stderr, matching watch's split). monitors.json had no hardcoded basename to fix.","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"tcp-p0x.15","depends_on_id":"tcp-p0x","type":"parent-child","created_at":"2026-07-25T01:47:03.469830871Z","created_by":"gulp","metadata":"{}","thread_id":""}]}
{"id":"tcp-p0x.16","title":"monitor: unified canonical git-mailbox watch backend","description":"## Goal\n\nMake the `agent-mail-monitor` watch read new mail from the append-only canonical git-mailbox on disk, watermark by message id, and notify across one / all / a product-scoped subset of an identity's projects, tagging each notification with its source project. Remove `am check-inbox` from the notification path.\n\n## Background: one root cause, many symptoms\n\nThe current watch polls `am check-inbox --agent <A> --project <dir>` (`src/core/am.ts` `pollInbox`). Two structural problems flow from that one choice:\n\n1. **check-inbox consumes.** Source-verified against am v0.3.21: without `--direct` the call takes the daemon path and invokes the server's `fetch_inbox`, whose contract is \"retrieve recent messages AND mark returned messages read.\" A daemon is normally up, so every poll marks the returned mail read and steals it from a later interactive `fetch_inbox(unread_only)`. The read-only SELECT path needs `--direct` AND no daemon; the plugin passes neither.\n2. **check-inbox is single-project.** Its only scoping flag is `--project` (one key). An identity receiving mail in multiple projects is watched in exactly one; the rest is silent. This bit live: WindyBarn's monitor was pinned to `tiny-claude-plugins` while its coordination thread lived in the vault project, and the replies never reached the monitor.\n\nBoth vanish by reading the canonical store instead. Every `send_message` writes the recipient inbox `.md` at delivery time (`<ROOT>/projects/<slug>/agents/<Agent>/inbox/YYYY/MM/<ts>__<subject>__<id>.md`), independent of SQLite. OS file reads never touch `read_ts` (genuinely read-only); the `.md` exists the instant mail is delivered (desync-immune); append-only + id watermark means no unread-only filter and no disappearing rows (also closes the tcp-p0x.12 transport hole). Cross-project falls out for free: the layout already partitions by `<slug>`, so watching all of an identity's projects is globbing more slug dirs, not a new am API.\n\nThe backend prototype already exists and was dogfooded live: `src/core/mailbox.ts` (`snapshotMailbox`, regression-tested in `mailbox_test.ts`) and `src/commands/shadow.ts` (Part A emitted `MAIL #<id>` correctly off this store, msgs 27657/27660/27661). This epic promotes it to production.\n\n## Supersedes / relates\n\nSupersedes tcp-p0x.6 (cross-project watch) and tcp-p0x.14 (FS backend swap) — both fold into this one change. Related: tcp-p0x.12 (transport hole), dissolved by the id-watermark append-only read.\n\n## Success Criteria\n\n- The notification path reads the canonical git-mailbox, not `check-inbox`; no mail is marked read by the watch.\n- A cross-project (`all`) mode notifies for mail in any of the identity's projects, tagged with the source project; default `project` mode is unchanged.\n- The private-layout coupling is guarded by a loud doctor check.\n- All child tasks closed and committed.","status":"closed","priority":1,"issue_type":"epic","created_at":"2026-07-25T03:15:43.182883849Z","created_by":"WindyBarn","updated_at":"2026-07-25T04:16:32.269278135Z","closed_at":"2026-07-25T04:16:32.269076753Z","close_reason":"All 6 children (.16.1–.16.6) landed & verified on main: 04b7be6, 32f6fd8, 11f7f4b, b700f64, 9c6dd25 (+.16.1 prior). FS-backend watch shipped: non-consuming, cross-project, source-tagged; check-inbox retired from notify path; doctor layout health + desync cross-check added; read-only claims reconciled.","source_repo":".","compaction_level":0,"original_size":0,"labels":["fs-backend"],"dependencies":[{"issue_id":"tcp-p0x.16","depends_on_id":"tcp-p0x","type":"parent-child","created_at":"2026-07-25T03:15:43.182883849Z","created_by":"WindyBarn","metadata":"{}","thread_id":""}]}
{"id":"tcp-p0x.16.1","title":"promote snapshotMailbox to the single-project watch loop","description":"## Goal\n\nReplace `pollInbox` (`am check-inbox`) in the single-project `monitor`/`watch` notification path with an id-watermarked `snapshotMailbox` poll. This is the foundation of the FS-backend epic; every other task builds on it.\n\n## Background\n\n`src/core/mailbox.ts` `snapshotMailbox` already reads the canonical git-mailbox layout `<ROOT>/projects/<slug>/agents/<Agent>/inbox/YYYY/MM/<ts>__<subject>__<id>.md`, parsing the numeric id off the filename tail (anchored regex, regression-tested in `mailbox_test.ts`). `ROOT` = `$AGENT_MAIL_MAILBOX_ROOT` or `~/.mcp_agent_mail_git_mailbox_repo`; `<slug>` = `slugForProject(cwd)` (matches am's dir-mode slugging, unit-tested). The `shadow` command (`src/commands/shadow.ts`) already emits notifications off this store — reuse that emission logic.\n\n## Constraints\n\n- OS file reads must never mark mail read (this is the whole point — verify `read_ts` is untouched after a poll).\n- On arm, seed the watermark from the current max id in scope (adopt-without-replay: only mail arriving after launch notifies).\n- The git-mailbox layout is am's PRIVATE on-disk contract — depend on it via `mailbox.ts` only; do not scatter path assumptions.\n\n## Acceptance Criteria\n\n- Arming the monitor in a single project notifies exactly once per new inbox `.md` whose id exceeds the watermark.\n- No message is marked read by the watch (`read_ts` unchanged across polls).\n- An empty inbox arms cleanly and stays live (no exit 4).\n- Existing `mailbox_test.ts` stays green; add a watch-loop test that a seeded watermark suppresses replay and a new file fires once.\n\n## Verification\n\n`deno test --allow-run --allow-env --allow-read --allow-write src/` green. Manually: arm against an inbox, send one message, confirm one notification and that `fetch_inbox(unread_only)` still returns that message (not consumed).","status":"closed","priority":1,"issue_type":"task","created_at":"2026-07-25T03:15:43.281191833Z","created_by":"WindyBarn","updated_at":"2026-07-25T03:31:28.141192209Z","closed_at":"2026-07-25T03:31:28.140988642Z","close_reason":"Landed 6e9e6d5: runWatch now polls snapshotMailbox (durable git-mailbox on disk) instead of check-inbox for the single-project watch/monitor path. Adopt-without-replay on arm, --since replay-once preserved, missing/empty inbox arms cleanly (no exit 4). Added watch_test.ts (3 tests: seeded-watermark suppresses replay, new file fires once, --since replay, empty inbox). Manually verified against real mailbox: one notification, message stayed unread until an explicit fetch_inbox. Full suite: deno test --allow-run --allow-env --allow-read --allow-write src/ -> 10 passed, 0 failed.","source_repo":".","compaction_level":0,"original_size":0,"labels":["fs-backend"],"dependencies":[{"issue_id":"tcp-p0x.16.1","depends_on_id":"tcp-p0x.16","type":"parent-child","created_at":"2026-07-25T03:15:43.281191833Z","created_by":"WindyBarn","metadata":"{}","thread_id":""}]}
{"id":"tcp-p0x.16.2","title":"add MAIL_WATCH_SCOPE=project|all|product scope config","description":"## Goal\n\nAdd a `MAIL_WATCH_SCOPE` env config (`project | all | product`, default `project`) that selects which project inboxes the FS watch (T1) snapshots.\n\n## Background\n\nThe canonical layout partitions by `projects/<slug>/agents/<Agent>/inbox/…`, so scope is just which slug dirs the snapshot globs. `project` = the current `CLAUDE_PROJECT_DIR` slug only (today's behavior). `all` = every `projects/<slug>` dir that has an inbox for this `$AGENT_NAME`. `product` = a configured/linked subset named by `MAIL_PRODUCT=<key>` (may split out if link-discovery is non-trivial — see Non-goals).\n\n## Constraints\n\n- Default `project` MUST be byte-identical to T1's single-project behavior — no regression for existing users.\n- Resolve slugs through `mailbox.ts` helpers; do not hand-roll path globbing in the command layer.\n\n## Non-goals\n\n- `product`-mode link discovery beyond an explicit `MAIL_PRODUCT` key — if discovering linked projects from am is non-trivial, file a follow-up and ship `project`+`all` first.\n\n## Acceptance Criteria\n\n- `MAIL_WATCH_SCOPE` unset or `project` reproduces T1 behavior exactly.\n- `MAIL_WATCH_SCOPE=all` notifies for a new message in ANY project the identity has an inbox in.\n- Invalid values fail loud with a clear message, not a silent fallback.\n\n## Verification\n\nUnit-test slug-set resolution for each scope. Manually: with two projects holding mail for one identity, `all` fires for both; `project` fires for only the armed one.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-07-25T03:15:43.402801619Z","created_by":"WindyBarn","updated_at":"2026-07-25T03:41:19.867118688Z","closed_at":"2026-07-25T03:41:19.866915732Z","close_reason":"landed 04b7be6: MAIL_WATCH_SCOPE=project|all|product for FS watch backend; product deferred/stubbed per non-goal","source_repo":".","compaction_level":0,"original_size":0,"labels":["fs-backend"],"dependencies":[{"issue_id":"tcp-p0x.16.2","depends_on_id":"tcp-p0x.16","type":"parent-child","created_at":"2026-07-25T03:15:43.402801619Z","created_by":"WindyBarn","metadata":"{}","thread_id":""},{"issue_id":"tcp-p0x.16.2","depends_on_id":"tcp-p0x.16.1","type":"blocks","created_at":"2026-07-25T03:15:43.402801619Z","created_by":"WindyBarn","metadata":"{}","thread_id":""}]}
{"id":"tcp-p0x.16.3","title":"tag notifications with source-project slug (cross-project delivery)","description":"## Goal\n\nEmit the source-project `<slug>` in each notification line so a human watching a multi-project `all`/`product` scope can tell which bus the mail landed on. This delivers the original tcp-p0x.6 acceptance criterion.\n\n## Background\n\nOnce T2 lets the watch span multiple projects, an untagged `MAIL #<id> from <sender>` line does not say which project it came from. The snapshot already knows the `<slug>` per file (it globbed it). Thread the slug through to the emission.\n\n## Constraints\n\n- Keep the per-message payload terse; add the slug as a compact tag, not a second line.\n- In single-project `project` scope the tag may be omitted or kept for consistency — decide and document.\n\n## Acceptance Criteria\n\n- In `all`/`product` scope, each notification names the source project (slug or full project-key).\n- The tag is consistent with the monitor-event label format used elsewhere.\n\n## Verification\n\nTrigger mail in two projects under `all` scope; confirm each notification identifies its source project distinctly.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-07-25T03:15:43.499763496Z","created_by":"WindyBarn","updated_at":"2026-07-25T03:49:03.194544374Z","closed_at":"2026-07-25T03:49:03.194250365Z","close_reason":"landed 32f6fd8: formalized + tested source-project slug tag in every scope (was already emitted by formatLine since T2, now documented + explicitly asserted for project and all)","source_repo":".","compaction_level":0,"original_size":0,"labels":["fs-backend"],"dependencies":[{"issue_id":"tcp-p0x.16.3","depends_on_id":"tcp-p0x.16","type":"parent-child","created_at":"2026-07-25T03:15:43.499763496Z","created_by":"WindyBarn","metadata":"{}","thread_id":""},{"issue_id":"tcp-p0x.16.3","depends_on_id":"tcp-p0x.16.2","type":"blocks","created_at":"2026-07-25T03:15:43.499763496Z","created_by":"WindyBarn","metadata":"{}","thread_id":""}]}
{"id":"tcp-p0x.16.4","title":"retire check-inbox from the notification path","description":"## Goal\n\nRemove `am check-inbox` (`pollInbox`) from the `watch` / `product` / `monitor` notification paths entirely, and update `src/cli.ts` wiring so the FS backend (T1) is the sole notification source.\n\n## Background\n\nOnce T1 is the backend, `check-inbox` in the notify path only reintroduces the consuming bug. Do NOT keep it as a \"read\" — the only non-consuming shape (`--direct` + no daemon) is not worth coupling to. Retain the `am.ts` adapter ONLY if T5's doctor cross-check needs a verified-non-consuming SQLite read; otherwise it can go too.\n\n## Constraints\n\n- Decide the fate of the legacy `scripts/watch-mail.sh` bash path: port it to the FS backend or deprecate it in favor of the Deno `cli.ts` path. Do not leave two divergent backends.\n\n## Acceptance Criteria\n\n- No notification path shells out to `check-inbox` — `grep -rn 'check-inbox' src/` shows only the doctor cross-check (if any) and dead-code-free.\n- `cli.ts` `monitor`/`watch`/`product` all route through the FS backend.\n- `deno check` + full test suite green.\n\n## Verification\n\nStatic: grep proves no `check-inbox` in the notify path. Dynamic: monitor still notifies correctly via the FS backend after removal.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-07-25T03:15:43.591883629Z","created_by":"WindyBarn","updated_at":"2026-07-25T04:10:33.310112242Z","closed_at":"2026-07-25T04:10:33.309881644Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["fs-backend"],"dependencies":[{"issue_id":"tcp-p0x.16.4","depends_on_id":"tcp-p0x.16","type":"parent-child","created_at":"2026-07-25T03:15:43.591883629Z","created_by":"WindyBarn","metadata":"{}","thread_id":""},{"issue_id":"tcp-p0x.16.4","depends_on_id":"tcp-p0x.16.1","type":"blocks","created_at":"2026-07-25T03:15:43.591883629Z","created_by":"WindyBarn","metadata":"{}","thread_id":""}]}
{"id":"tcp-p0x.16.5","title":"doctor: mailbox-layout health check + non-consuming desync cross-check","description":"## Goal\n\nGuard the private-layout coupling: add a `doctor` check that resolves the mailbox root and verifies the git-mailbox layout parses, failing LOUD if am's private layout shifts. Repurpose the shadow Part B divergence detector as an optional, on-demand desync cross-check.\n\n## Background\n\nThe git-mailbox layout is am's PRIVATE on-disk contract; if it changes shape, the FS backend silently sees no mail. A doctor check that parses the layout turns a silent breakage into a loud, diagnosable failure. Shadow Part B (`src/commands/shadow.ts`) already compares the canonical store against a SQLite read to detect divergence — repurpose it to flag when SQLite lags canonical (the DB-desync blind spot), as a health signal, never in the notification path.\n\n## Constraints\n\n- The desync cross-check MUST use a re-verified-non-consuming SQLite read. `am robot inbox` is a candidate but a \"read-only\" label is exactly what proved false for `check-inbox` — re-verify at the am source before wiring it in.\n- On-demand only; must never mark mail read.\n\n## Acceptance Criteria\n\n- `doctor` fails loud (non-zero, named failure) if the mailbox root is missing or the layout does not parse.\n- The desync cross-check is available on demand, never consumes, and flags SQLite-lags-canonical.\n\n## Verification\n\nPoint the check at a well-formed root (passes) and a malformed one (fails loud). Confirm the cross-check leaves `read_ts` untouched.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-07-25T03:15:43.693768466Z","created_by":"WindyBarn","updated_at":"2026-07-25T04:03:28.165422333Z","closed_at":"2026-07-25T04:03:28.165225028Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["fs-backend"],"dependencies":[{"issue_id":"tcp-p0x.16.5","depends_on_id":"tcp-p0x.16","type":"parent-child","created_at":"2026-07-25T03:15:43.693768466Z","created_by":"WindyBarn","metadata":"{}","thread_id":""},{"issue_id":"tcp-p0x.16.5","depends_on_id":"tcp-p0x.16.1","type":"blocks","created_at":"2026-07-25T03:15:43.693768466Z","created_by":"WindyBarn","metadata":"{}","thread_id":""}]}
{"id":"tcp-p0x.16.6","title":"retract residual read-only claims; assert FS backend is read-only","description":"## Goal\n\nSweep the last surfaces that still assert `check-inbox` is read-only, and — once T4 lands — restate the notification path as genuinely non-consuming (because it now is).\n\n## Background\n\nAn earlier interim sweep (commits 32a4393 / 9544e85 / 806f922) corrected `src/core/am.ts`, `scripts/watch-mail.sh` header, `skills/toggle/SKILL.md`, `monitors/monitors.json`, and `agents/mail-triage.md` to WARN that check-inbox consumes. Two surfaces remain: `.claude-plugin/plugin.json` (\"Read-only Agent Mail surface\" self-description) and the `scripts/watch-mail.sh:41` usage heredoc (\"read-only\" + \"does NOT mark messages read\"). After T4 removes check-inbox from the notify path, the accurate claim flips positive: the FS backend IS read-only.\n\n## Constraints\n\n- Do not re-assert read-only for any surface that still routes through `check-inbox` (e.g. an on-demand doctor cross-check, unless T5 verified it non-consuming).\n\n## Acceptance Criteria\n\n- `grep -rin 'read-only\\|does not mark' plugins/agent-mail-monitor/` returns no FALSE claim about `check-inbox`.\n- The plugin self-description and toggle skill accurately state the FS-backed notification path is non-consuming.\n\n## Verification\n\nGrep sweep clean; `skill-check` still PASS on any touched skill.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-07-25T03:15:43.800039772Z","created_by":"WindyBarn","updated_at":"2026-07-25T04:13:59.734102173Z","closed_at":"2026-07-25T04:13:59.733858020Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"labels":["fs-backend"],"dependencies":[{"issue_id":"tcp-p0x.16.6","depends_on_id":"tcp-p0x.16","type":"parent-child","created_at":"2026-07-25T03:15:43.800039772Z","created_by":"WindyBarn","metadata":"{}","thread_id":""},{"issue_id":"tcp-p0x.16.6","depends_on_id":"tcp-p0x.16.4","type":"blocks","created_at":"2026-07-25T03:15:43.800039772Z","created_by":"WindyBarn","metadata":"{}","thread_id":""}]}
{"id":"tcp-p0x.2","title":"doctor mcp-declaration: four-state fix (handle rejected server)","description":"## Goal\nFix the doctor's `mcp-declaration` remedy so it resolves the most common real failure — a\nserver that is declared but REJECTED — instead of prescribing a fix that cannot work.\nGrayBrook ranked this the one real bug in the review.\n\n## Background\nWhen the agent's MCP mail tools are missing, doctor emits\n`[WARN] mcp-declaration → resources/declare-mcp.md`. That resource offers two fixes: run `am`\n(auto-detects agents, refreshes MCP), or `claude mcp add --scope …`. Neither clears a prior\nrejection, which persists in `.claude/settings.local.json`:\n`\"disabledMcpjsonServers\": [\"mcp-agent-mail\"]`. Adding a fresh declaration on top of a\nrejected one leaves it rejected — a dead end. `claude mcp get <name>` names the state outright\n(\"Status: ✘ Rejected (see disabledMcpjsonServers in settings)\").\n\n## Steps to Reproduce\n1. In a project whose `.mcp.json` declares agent-mail, reject it at the approval prompt (or add\n   `mcp-agent-mail` to `disabledMcpjsonServers` in `.claude/settings.local.json`).\n2. Start a session; MCP mail tools are absent.\n3. Run the doctor skill → `[WARN] mcp-declaration → declare-mcp.md`.\n4. Follow declare-mcp.md (`am`, or `claude mcp add`). The tools remain absent; nothing the\n   file prescribes removes the `disabledMcpjsonServers` entry.\n\n## Acceptance Criteria\n- The mcp-declaration check runs `claude mcp get <name>` and branches on Status across four\n  states, each with the correct fix:\n  - absent → today's guidance (`am`, or `claude mcp add`)\n  - `✘ Rejected` → remove the name from `disabledMcpjsonServers` in `.claude/settings.local.json`\n  - `⏸ Pending approval` → approve at next startup / `/reload-plugins`; no config edit\n  - `✔ Connected` → pass\n- `declare-mcp.md` carries the four-state table.\n- Secondary: replace the `grep -i agent-mail` substring match (over-matches unrelated names)\n  with an exact-name check, and drop the hard `/mcp/` path assumption (verify against\n  `am status --json .service.http_url`; GrayBrook's server served `/api/`).\n\n## Verification\nReproduce the rejected state, run the patched doctor, confirm it names the rejected state and\nthe settings-file remedy; `/reload-plugins` brings tools up without a restart.","status":"closed","priority":1,"issue_type":"bug","created_at":"2026-07-24T18:18:13.488716269Z","created_by":"gulp","updated_at":"2026-07-25T03:34:00.452071568Z","closed_at":"2026-07-25T03:34:00.451862020Z","close_reason":"Committed c3fc916: mcp-declaration check now runs 'claude mcp get mcp-agent-mail' and branches on all four states (absent/Rejected/Pending/Connected), each with the correct fix; declare-mcp.md carries the four-state table; standardized server name to mcp-agent-mail; dropped hardcoded /mcp/ path assumption in favor of am status --json .service.http_url. Verified via scratch-project repro of all four states plus deno fmt/check/test + shellcheck + skill-check, all clean.","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"tcp-p0x.2","depends_on_id":"tcp-p0x","type":"parent-child","created_at":"2026-07-24T18:18:13.488716269Z","created_by":"gulp","metadata":"{}","thread_id":""}]}
{"id":"tcp-p0x.3","title":"toggle ON path: fix stale mail-monitor.sh entrypoint + add mid-session identity route","description":"## Goal\nFix the `toggle` skill's \"Turn it ON\" path: it points at the SUPERSEDED bash entrypoint and\noffers no mid-session identity route — the exact situation that sends a user to the skill.\n\n## Background\nTwo defects in skills/toggle/SKILL.md \"Turn it ON\":\n1. WRONG ENTRYPOINT. It arms `\"${CLAUDE_PLUGIN_ROOT}\"/scripts/mail-monitor.sh`. The 0.4.0\n   monitor entrypoint is the Deno CLI: `deno run --allow-run=am --allow-env --allow-read\n   \"${CLAUDE_PLUGIN_ROOT}/src/cli.ts\" monitor` (see monitors/monitors.json). `mail-monitor.sh`\n   is the retired bash shim slated for removal. Confirmed live this session — arming had to\n   use the Deno entrypoint, contradicting the skill.\n2. NO MID-SESSION ROUTE. The ON path says only \"Set one at launch: AGENT_NAME=YourName\", but\n   the reason you reach the toggle skill is that you are ALREADY mid-session with no identity;\n   relaunching is precisely what you cannot do. The working move — prefixing the env onto the\n   Monitor command — is undocumented:\n   `AGENT_NAME=<name> CLAUDE_PROJECT_DIR=<path> deno run … src/cli.ts monitor`\n   (the CLI reads both from its own environment, so this arms correctly in a live session).\n\n## Steps to Reproduce\n1. Launch a session with no AGENT_NAME.\n2. Invoke the toggle skill and follow \"Turn it ON\".\n3. It points at `scripts/mail-monitor.sh` (wrong for 0.4.0) and tells you to relaunch — which\n   you cannot do mid-session.\n\n## Acceptance Criteria\n- The ON path arms the 0.4.0 Deno entrypoint (`src/cli.ts monitor`), not `mail-monitor.sh`.\n- It documents the mid-session env-prefix arming move (AGENT_NAME + CLAUDE_PROJECT_DIR on the\n  command).\n- The OFF path's \"find the task running mail-monitor.sh\" language is updated to the Deno\n  command label.\n- `skill-check check skills/toggle --strict --no-security-scan` → PASS.\n- Coordinate with the human-only removal of the superseded scripts/{mail-monitor,watch-mail}.sh\n  so the skill and the shipped files agree.\n\n## Verification\nRe-arm from a no-identity session by following the patched skill verbatim; the monitor starts\non the Deno entrypoint and watches the given identity.","status":"closed","priority":1,"issue_type":"bug","created_at":"2026-07-24T18:18:13.608544732Z","created_by":"gulp","updated_at":"2026-07-24T18:53:55.948378864Z","closed_at":"2026-07-24T18:53:55.948181879Z","close_reason":"Fixed toggle ON path: arm the 0.4.0 Deno entrypoint (deno run … src/cli.ts monitor, matching monitors.json) instead of the retired scripts/mail-monitor.sh; OFF path now IDs the task by its stable agent-mail-inbox label; corrected the 'needs jq' claim (0.4.0 monitor parses am JSON with Zod, no jq); added a mid-session identity route (AGENT_NAME=… command prefix, since a later shell export doesn't reach the running watch). skill-check 100/100 PASS.","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"tcp-p0x.3","depends_on_id":"tcp-p0x","type":"parent-child","created_at":"2026-07-24T18:18:13.608544732Z","created_by":"gulp","metadata":"{}","thread_id":""}]}
{"id":"tcp-p0x.4","title":"identity-picking: list existing agents (am agents list -p) when AGENT_NAME unset","description":"## Goal\nWhen AGENT_NAME is unset, help the user PICK an existing registered identity instead of\nminting a new one — surfacing the project's current agents so they reuse one and avoid\norphaning reservations.\n\n## Background\nThe `agent-name` check warns that AGENT_NAME is unset; `set-agent-name.md` says pick a durable\nadjective+noun and use it every session. Both are correct but incomplete: a project often\nalready has registered identities, and the right move is to REUSE one, not mint a fourth. The\nplugin's own docs warn that \"a new name mints a new empty mailbox and orphans prior\nreservations\" — so the fix actively steers away from a failure mode the docs already name.\nGrayBrook's project had three identities (GrayBrook, QuietFalcon, CrimsonMaple); the doctor\nnever showed them.\n\nNote: `set-agent-name.md` uses `PlumHare` as its concrete example name. This literal leaked\ninto a coordinator's session notes as a phantom \"real\" identity that did not exist — consider\nmaking the example obviously illustrative (or generating one) to avoid that trap.\n\n## Constraints\n- Read-only. Just list; never register on the user's behalf.\n- `am agents list` currently needs `-p <path>` (a positional key is rejected) — use the flag.\n\n## Acceptance Criteria\n- When AGENT_NAME is unset, the doctor (and the onboarding skill's identity step) run\n  `am agents list -p <project-key>` and print the candidate identities (name, model,\n  last-active), framed as \"reuse one of these, or mint a new adjective+noun\".\n- The doctor is the natural home — it already knows the project key.\n- `set-agent-name.md`'s `PlumHare` example is made clearly illustrative (or the guidance points\n  at the live list instead of a hard-coded name).\n\n## Verification\nIn a project with ≥1 registered agent and no AGENT_NAME, the doctor lists the existing\nidentities; the onboarding step references the same list.","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-07-24T18:18:13.738585195Z","created_by":"gulp","updated_at":"2026-07-25T08:11:37.124806029Z","closed_at":"2026-07-25T08:11:37.124513593Z","close_reason":"identity-picking hint landed: doctor lists registered agents (name/model/last-active) when AGENT_NAME unset","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"tcp-p0x.4","depends_on_id":"tcp-p0x","type":"parent-child","created_at":"2026-07-24T18:18:13.738585195Z","created_by":"gulp","metadata":"{}","thread_id":""}]}
{"id":"tcp-p0x.5","title":"polish: exit-3 framing, health WARN probe-naming, DB-lock hint; report am positional-key upstream","description":"## Goal\nTriage the polish-tier findings from GrayBrook's review — small framing/CLI clarity items, and\none upstream `am` note that is out of scope to fix here.\n\n## Background\nLower-ranked findings from message #27588, grouped:\n\n1. exit-3 framing. The monitor auto-arms, finds no AGENT_NAME, exits 3 — which the harness\n   surfaces as `status: failed` (\"script failed (exit 3)\"). The stdout notice is excellent and\n   loud, but \"failed\" reads as a crash and prompts a \"was mail dropped?\" check (it wasn't —\n   read-only by design). If the harness allows, distinguish \"idle / not armed\" from \"failed\"\n   with the same loudness minus the false alarm. Deliberate design choice — evaluate, don't\n   assume a change.\n2. health WARN overstates. `[WARN] health — am health: overall=unhealthy level=green` sent\n   GrayBrook chasing a disagreement: `am doctor check --json` says `healthy: true`, while\n   `am health` flags one probe, `archive_db_parity` (archive 152 projects vs DB 133; messages\n   differ by 6 of 6223 — old sandbox eval dirs, no mail at risk). Fix: name the failing probe\n   in the WARN line (`health — am health: unhealthy (probe: archive_db_parity)`); when parity\n   is the only failing probe, arguably INFO, not WARN.\n3. DB-lock contention on mutating ops. A mutating `am` op (contact approve / mail reply) can be\n   blocked by an exclusive activity lock held by the running server; the MCP path times out\n   under the same contention (observed both by GrayBrook and by the coordinator filing this).\n   Consider surfacing a clearer \"server busy — retry / owner pid\" hint in doctor, and document\n   the \"verify state, don't blind-retry\" discipline.\n\n## Non-goals / upstream\n- `am agents list` (and `am agents show`) rejecting a POSITIONAL project key with no hint to\n  use `-p` is an upstream `am` CLI issue — report upstream, do not fix in the plugin.\n\n## Acceptance Criteria\n- Items 1–3 each either land a small plugin/doc change or are explicitly closed as\n  won't-fix/deliberate with a one-line rationale.\n- The `am agents list` positional-key issue is filed/reported upstream (link or note), not\n  silently absorbed.\n\n## Verification\nDoctor output reflects any adopted framing/probe-naming changes; a note records the upstream\nreport and any deliberate no-ops.","status":"closed","priority":3,"issue_type":"task","created_at":"2026-07-24T18:18:13.866510810Z","created_by":"gulp","updated_at":"2026-07-25T08:29:57.786426604Z","closed_at":"2026-07-25T08:29:57.786223628Z","close_reason":"Triaged all three review items + filed the upstream note. Details:\n\n1. exit-3 framing (src/cli.ts monitor entrypoint) — LANDED a message change, KEPT the exit\n   code as a deliberate won't-fix. The idle notice now adds an explicit line: \"this is NOT a\n   crash and no mail was dropped — the backend is read-only and never consumes mail; it simply\n   has no identity to watch under yet.\" Evaluated switching NO_IDENTITY(3) to exit 0 (Monitor's\n   \"clean end\") to dodge the harness's blanket nonzero->status:failed label, but declined: 3 is\n   this CLI's ONLY producer of NO_IDENTITY and is part of the documented, repo-wide exit-code\n   contract (top-level CLAUDE.md Conventions: 2=usage, 3=missing identity/env, 4=first-poll-\n   failed) — repurposing it to mean \"success\" here would silently break that contract for any\n   other caller that branches on $?. The harness's status label is outside this CLI's control\n   either way; docs (toggle/SKILL.md, doctor/resources/set-agent-name.md) already describe the\n   code-3-loud-notice behavior accurately and needed no change.\n\n2. health WARN overstates (skills/doctor/scripts/doctor.sh) — LANDED. The WARN line now names\n   the actual failing probe(s) from `am health --json`'s `.probes[]` (falls back to the old\n   generic overall=/level= wording if `.probes` is absent/unparseable). Verified live against\n   the real am service: `[WARN] health — am health: unhealthy (probe: archive_db_parity) —\n   inspect with 'am health --json' / 'am doctor check'` (confirmed archive_db_parity was the\n   sole failing probe among db_connectivity/db_file_sanity/db_schema/search_index/\n   circuit_breakers/backpressure/integrity/disk/tui_liveness). Considered promoting a\n   parity-only drift to an INFO tier instead of WARN — declined: doctor.sh's severity model\n   (PASS/WARN/FAIL) has no INFO tier anywhere in this plugin, and adding one just for this one\n   probe is scope creep for a P3 polish item; naming the probe already gives the reader enough\n   to judge severity themselves.\n\n3. DB-lock contention hint (doctor.sh + skills/doctor/resources/server-down.md +\n   skills/doctor/SKILL.md) — LANDED as documentation + a wording enrichment, not a new live\n   probe. doctor.sh's `server` WARN detail now names lock contention as a possible cause\n   alongside \"stopped\", pointing at server-down.md. server-down.md gained a new section on\n   mutating-op DB-lock contention: symptom, and the \"verify state via\n   diagnose-agent-mail-service, don't blind-retry\" discipline, since retrying a mutating call\n   against a still-held lock just adds a contender rather than clearing it. SKILL.md's severity\n   section got the same pointer. Declined a live \"server busy / owner pid\" probe in doctor.sh\n   itself: doctor is deliberately read-only, and detecting an active mutating-op lock without\n   attempting a mutating op (or reaching into am's internals) isn't something a read-only probe\n   can do safely — the existing diagnose-agent-mail-service skill's systemd Main-PID probe is\n   the closest thing to an \"owner pid\" this plugin can report, and the new doc section points at\n   it explicitly.\n\nUpstream (reported here, not fixed in-plugin): `am agents list <path>` (and `am agents show`)\nrejects a POSITIONAL project key with a bare clap error and no hint to use -p/--project.\nReproduced live:\n\n    $ am agents list /home/gulp/projects/tiny-claude-plugins\n    error: unexpected argument '/home/gulp/projects/tiny-claude-plugins' found\n    Usage: am agents list [OPTIONS] --project <PROJECT_KEY>\n\nSuggested upstream fix: either accept the bare positional as an alias for --project, or have\nclap's error path suggest `-p/--project <key>` explicitly. No am/mcp-agent-mail issue tracker\nreachable from this session — a human should file this against that project directly.\n\nVerification: `deno fmt`, `deno check src/cli.ts`, `bash -n doctor.sh`, full\n`deno test --allow-run --allow-env --allow-read --allow-write src/` (47 passed), and\n`claude plugin validate .` all pass. doctor.sh was also run live against the real am service to\nconfirm the new probe-naming output.","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"tcp-p0x.5","depends_on_id":"tcp-p0x","type":"parent-child","created_at":"2026-07-24T18:18:13.866510810Z","created_by":"gulp","metadata":"{}","thread_id":""},{"issue_id":"tcp-p0x.5","depends_on_id":"tcp-p0x.15","type":"blocks","created_at":"2026-07-25T08:04:40.356715963Z","created_by":"gulp","metadata":"{}","thread_id":""},{"issue_id":"tcp-p0x.5","depends_on_id":"tcp-p0x.4","type":"blocks","created_at":"2026-07-25T08:04:40.187453687Z","created_by":"gulp","metadata":"{}","thread_id":""}]}
{"id":"tcp-p0x.6","title":"monitor: cross-project / product-scoped watch (identity receives mail in multiple projects)","description":"## Goal\nLet the monitor watch an identity's mail across ALL its projects (or a product's linked\nprojects), not just the single `CLAUDE_PROJECT_DIR` it was armed with. A coordinator identity\nthat receives mail in more than one project currently gets notified for only one — the rest is\na silent blind spot.\n\n## Background\nThe monitor polls `am check-inbox --agent <A> --project <CLAUDE_PROJECT_DIR>` (src/core/am.ts).\n`check-inbox` is single-project: its only scoping flag is `--project` (one key), with no\nproduct/cross-project mode. So the watch sees exactly one project's inbox.\n\nThis bit for real. WindyBarn's monitor was armed with\n`CLAUDE_PROJECT_DIR=/home/gulp/projects/tiny-claude-plugins`, but the entire GrayBrook↔WindyBarn\nthread (msgs 27588 / 27589 / the follow-up) lives in the VAULT project\n(`/home/gulp/Vaults/everything`). GrayBrook's reply could not reach the monitor — confirmed at\nthe OS level (running deno pid polls the tiny-claude-plugins project only). The recipient never\ngets a \"new mail\" signal for mail that landed in a project the monitor isn't pinned to.\n\nThis is the monitor-side twin of tcp-p0x.1: the same \"one identity, many project-ids\" friction,\non the WATCH path instead of the LOOKUP path. Both stem from `am`'s per-project scoping.\n\n## Design sketch\n- `am` already exposes a cross-project surface: `am robot overview` returns per-project\n  unread/urgent/ack COUNTS across projects. Use it as the cheap cross-project detector: poll\n  overview for the agent; when any project's unread count rises, drill into THAT project with\n  `check-inbox` to fetch the new message details and emit the notification (tagged with which\n  project it came from).\n- Alternatively/additionally, a product-scoped mode: `am products` links projects into a\n  product; MCP has `fetch_inbox_product`. When the identity's projects are a linked product,\n  watch the product.\n- Config: a monitor option (env, e.g. `MAIL_WATCH_SCOPE=project|all|product` +\n  `MAIL_PRODUCT=<key>`), defaulting to today's single-project behavior so nothing regresses.\n- Read-only throughout — overview and check-inbox both leave messages unread.\n\n## Steps to Reproduce\n1. Register one identity (WindyBarn) that receives mail in two projects (A = tiny-claude-plugins,\n   B = vault).\n2. Arm the monitor with `CLAUDE_PROJECT_DIR=A`.\n3. Have someone send WindyBarn a message in project B.\n4. The monitor never notifies — it polls only A's inbox.\n\n## Acceptance Criteria\n- A cross-project (or product-scoped) watch mode emits a notification for a new message in ANY\n  of the identity's projects, tagging the source project in the notification line.\n- Default behavior (single project) is unchanged when the new mode is not requested.\n- Still read-only: no message is marked read by the watch.\n- A test covers \"message in a non-armed project triggers a notification\" (the exact miss above).\n\n## Verification\nReproduce the two-project setup; a message in the non-armed project produces a notification\nnaming that project; `deno test --allow-write` covers the cross-project detection path.\n\n## Notes\n- Pairs with tcp-p0x.1 (global message resolver): together they close the read + watch halves of\n  the coordinator-across-project-ids gap.\n- Aside observed while diagnosing: a stale 0.3.0 monitor (identity AzureAnchor,\n  everything-tooling) is still running the RETIRED scripts/mail-monitor.sh — unrelated leftover,\n  reinforces tcp-p0x.3's entrypoint-retirement cleanup.","status":"closed","priority":1,"issue_type":"feature","created_at":"2026-07-24T18:28:28.389320028Z","created_by":"gulp","updated_at":"2026-07-25T03:15:58.383662321Z","closed_at":"2026-07-25T03:15:58.383465387Z","close_reason":"Superseded by epic tcp-p0x.16 (unified canonical git-mailbox watch backend). Cross-project watch is delivered by tcp-p0x.16.2 (MAIL_WATCH_SCOPE) + tcp-p0x.16.3 (source-project tagging) — it falls out of the FS backend for free rather than needing a separate check-inbox feature.","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"tcp-p0x.6","depends_on_id":"tcp-p0x","type":"parent-child","created_at":"2026-07-24T18:28:28.389320028Z","created_by":"gulp","metadata":"{}","thread_id":""}],"comments":[{"id":1,"issue_id":"tcp-p0x.6","author":"WindyBarn","text":"## Design decision (prep, WindyBarn 2026-07-25): unify onto the git-mailbox FS backend — .6 ∧ .14 converge\n\nDecided after grounding against live state + QuietHeron's v0.4.0 field audit (msg #27683).\n\n### Decision\nUse ONE substrate for both the cross-project *detector* and the per-project *drill*:\nthe canonical git-mailbox filesystem (the `mailbox.ts` reader), NOT `am robot overview`\n(SQLite counts) as detector + `check-inbox` as drill.\n\nRationale — mixing SQLite-counts detection with an FS/check-inbox drill is two sources of\ntruth that can desync (the canonical/DB desync mailbox.ts's own header cites, msg 27651).\nThe git-mailbox layout `<ROOT>/projects/<slug>/agents/<Agent>/inbox/YYYY/MM/*.md` already\npartitions by project, so \"watch an identity across ALL its projects\" is: glob\n`projects/*/agents/<Agent>/inbox/**`, watermark on filenames, emit on each new file tagged\nwith its `<slug>`.\n\n### What this buys by construction\n- Non-consuming — reading files never marks read (dissolves QuietHeron item 2: check-inbox\n  daemon-up routes through fetch_inbox and MARKS READ; cross-project drilling would eat mail\n  in unwatched projects).\n- Empty-safe — empty inbox = no files; QuietHeron item 1 (empty stdout → JSON.parse(\"\") →\n  exit 4) never arises because there is no check-inbox in the notification path.\n- Cross-project is trivial — globbing more project dirs, not a new am API mode.\n- Coherent with tcp-p0x.15 — the per-message notification is tagged with the project slug it\n  appeared under, which IS the bus-identity label .15 asks for.\n- No throwaway — building .6 on check-inbox then ripping it out for .14 is two changes on a\n  substrate being retired; folding .6 onto the FS reader makes .6 and .14 ONE change.\n\n### Config\n`MAIL_WATCH_SCOPE = project | all | product`, default `project` (back-compat with today's\nsingle-project watch). `all` = every project the identity has an inbox dir in. `product` =\na configured/linked subset.\n\n### Honest caveat\nCouples to Agent Mail's PRIVATE on-disk layout (mailbox.ts header says so). Meant to converge\nto `am products inbox --since-ts` once the upstream Product Bus lands. Accepted interim\nsubstrate, not a committed contract.\n\n### Sequencing dependency (blocks start of code)\nThis builds on the currently UNTRACKED `src/core/mailbox.ts`, `src/core/mailbox_test.ts`,\n`src/commands/shadow.ts` — no attributed owner on this bus (QuietHeron explicitly changed no\ncode; GoldenCoyote's work was the coordinate skill, committed). Almost certainly the human's\nown shadow experiment. .6-as-FS-backend cannot land until those are committed/attributed and\nthe .14 direction is accepted. Prerequisite regardless of path: apply QuietHeron item 1's\nempty-inbox guard if check-inbox stays anywhere in the codebase.\n\n### Backend chosen for the drill\ngit-mailbox FS reader (mailbox.ts), unified with the detector. (Chosen by gulp: \"pick the\nelegant option for a coherent design.\")\n","created_at":"2026-07-25T02:19:04Z"}]}
{"id":"tcp-p0x.7","title":"monitor: ack/urgency-aware watch + opt-in ack subscription (robot inbox backing)","description":"## Goal\nMake the monitor ack/urgency-aware: (a) tag ack-required (and urgent) messages in the\nnotification line, and (b) offer an opt-in \"subscribe to acks\" watch mode that notifies ONLY on\nack-required / urgent mail. Read-only throughout.\n\n## RE-SCOPE 2026-07-25 (premise correction — READ FIRST)\nThis bead was authored when the watch detector was `am check-inbox`. It no longer is.\n`tcp-p0x.16.1` promoted `snapshotMailbox` (the git-mailbox FS reader) to the single-project\nwatch loop and `tcp-p0x.16.4` **retired `check-inbox` entirely** from every notification path.\nSo the original plan below — \"keep check-inbox as the default detector, add `am robot inbox` as\nan opt-in enrichment\" — is DEAD on both ends: there is no check-inbox default to preserve, and\nthe enrichment source is no longer forced to be `am robot inbox`.\n\n**New backing source: the canonical `.md` frontmatter (FS-native).** Verified 2026-07-25 against\na live mailbox file: every canonical message `.md` carries `ack_required` (bool) and `importance`\n(low|normal|high|urgent) in its `---json` frontmatter. `snapshotMailbox` already holds the\non-disk `path` to each message (`MailboxEntry.path`), so the marker + filter data is one\nfrontmatter parse away — no new command, no daemon, genuinely read-only, and it sees the SAME\nmessage set as the detector.\n\n**Why NOT `am robot inbox` for the basic marker** (this bead's own earlier discovery, now the\nargument for FS-native): `am robot inbox` is the ACTIONABLE (filtered) view — for an\nalready-engaged thread it returns `count: 0` where the raw mailbox still lists the message.\nBacking the marker with robot-inbox would therefore silently DROP the marker on exactly the mail\nthe FS detector still surfaces. The frontmatter route has no such mismatch. `am robot inbox` may\nstill be an OPTIONAL later enrichment for ack-OVERDUE SLA semantics (the 30-min overdue clock is\nsynthesized, not a static frontmatter field) — but it is not the backing for the basic ack/urgent\nmarker, and it must never gate the detector.\n\n## Design sketch (re-scoped)\n- `MAIL_WATCH_MODE=basic|actionable`, default `basic` = today's FS-watch line, unchanged (no\n  regression).\n- In `actionable` mode, extend the FS reader to parse `ack_required` + `importance` from each new\n  message's `.md` frontmatter (at `MailboxEntry.path`) and render a marker on the notification\n  line, e.g. `MAIL #<id> [<project>] <ts> (ACK-REQUIRED): <subject>` /\n  `… (URGENT): …`. Keep the existing `formatLine` shape; the marker is additive.\n- Subscription filter: `MAIL_WATCH_FILTER=ack|urgent` fires the watch ONLY on\n  ack-required (or high/urgent-importance) messages — the \"subscribe to acks\" feature, computed\n  from the same frontmatter fields.\n\n## Constraints\n- cli.ts CHOKEPOINT: this bead edits `cli.ts` (env/mode wiring), `commands/watch.ts`\n  (`formatLine` + loop), and `core/mailbox.ts` (frontmatter parse on `MailboxEntry`). Serialized\n  behind `tcp-p0x.5` (also cli.ts) via a `blocks` edge — do not run them in parallel.\n- Still read-only: FS frontmatter parse touches nothing; no `read_ts` mutation.\n\n## Acceptance Criteria\n- Default (basic) mode is byte-for-byte unchanged (FS watch, current notification format).\n- `actionable` mode tags ack-required / urgent messages in the notification line, sourced from\n  the canonical `.md` frontmatter (NOT check-inbox, NOT robot-inbox).\n- A filter mode (`MAIL_WATCH_FILTER=ack|urgent`) notifies only on ack-required (or urgent)\n  messages — the \"subscription\".\n- Still read-only (no message marked read by the watch — verify).\n- Tests cover: (1) an ack-required message gets the marker; (2) filter mode suppresses a\n  non-ack message; (3) basic mode output is unchanged.\n\n## Verification\n`deno test --allow-read --allow-write` covers the three cases (fixture `.md` files with\n`ack_required:true` / `importance:urgent` frontmatter). `deno fmt` + `deno check src/cli.ts`\nclean. A live run in a project with an ack-required message shows the marker (and, in filter mode,\nonly that message).\n\n## Notes\n- Sibling of tcp-p0x.6 (cross-project watch): both are \"make the watch smarter about which mail\n  matters\". A full coordinator watch eventually wants BOTH cross-project scope AND ack/urgency\n  awareness.\n- Title still reads \"(robot inbox backing)\" — stale after this re-scope; backing is now FS\n  frontmatter. Left in the title only to preserve the id-visible history; the body is\n  authoritative.","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-07-24T18:28:28.539994697Z","created_by":"gulp","updated_at":"2026-07-25T08:38:24.284770011Z","closed_at":"2026-07-25T08:38:24.284555523Z","close_reason":"Landed: MAIL_WATCH_MODE=basic|actionable + MAIL_WATCH_FILTER=ack|urgent, backed by the canonical .md frontmatter (ack_required/importance), not check-inbox/robot-inbox. See commit for details.","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"tcp-p0x.7","depends_on_id":"tcp-p0x","type":"parent-child","created_at":"2026-07-24T18:28:28.539994697Z","created_by":"gulp","metadata":"{}","thread_id":""},{"issue_id":"tcp-p0x.7","depends_on_id":"tcp-p0x.15","type":"blocks","created_at":"2026-07-25T08:04:40.504393837Z","created_by":"gulp","metadata":"{}","thread_id":""},{"issue_id":"tcp-p0x.7","depends_on_id":"tcp-p0x.5","type":"blocks","created_at":"2026-07-25T08:20:44.659794058Z","created_by":"gulp","metadata":"{}","thread_id":""}]}
{"id":"tcp-p0x.8","title":"AM broadcast: reconcile flag-works vs robot-docs-says-unsupported drift (report upstream + fix plugin swarm docs)","description":"## Goal\nReconcile a documentation-vs-implementation drift in Agent Mail's broadcast surface, and\nrecord the resolved behavior where the plugin's swarm docs can rely on it. Report the doc\ndrift upstream (mcp_agent_mail_rust); do NOT change am source.\n\n## Background\nSurfaced while researching how the AM server supports swarms (design note\n`design/agent-mail-swarm-model-and-robot-surface.md` in the vault). Three sources disagree,\nand the disagreement was verified against source:\n\n- **The tool layer supports broadcast.** `send_message` takes `broadcast: Option<bool>`\n  (`crates/mcp-agent-mail-tools/src/messaging.rs`). With `broadcast=true` and an empty `to`,\n  the server expands `to` to every project agent active in the last 30 days, minus the sender\n  and anyone with `contact_policy=block_all` (messaging.rs ~1011-1032). Mutually exclusive with\n  an explicit `to`. Unit-tested (messaging.rs ~2567).\n- **The pseudo-recipient route is rejected.** Naming an agent `all`/`everyone`/`broadcast`/`*`\n  as a recipient errors \"Agent Mail doesn't support broadcasting to all agents\"\n  (`BROADCAST_TOKENS` / `looks_like_broadcast`, `crates/mcp-agent-mail-core/src/models.rs`) —\n  that error's job is to steer callers to the flag.\n- **The agent-facing guide disclaims it.** `am robot-docs guide` (Coordination Safety) says\n  \"Broadcast send_message is intentionally unsupported.\"\n\nNet: the flag works; the guide is stale/conservative relative to it. A caller that wants\nfan-out today (e.g. agent-mail-coordinator's `amc broadcast`) implements N individual sends\nclient-side because the guide told it broadcast was unsupported.\n\n## Non-goals\n- Do NOT modify am / mcp_agent_mail_rust source — the flag behavior is correct as-is.\n- Not a plugin code change; this is a knowledge-reconciliation + upstream-report task.\n\n## Acceptance Criteria\n- The drift is reported upstream (issue/note against mcp_agent_mail_rust: the `robot-docs guide`\n  \"broadcast intentionally unsupported\" line contradicts the working, tested `broadcast` flag) —\n  link or reference recorded here.\n- The plugin's swarm docs (e.g. `driving-swarms-with-mail`) either (a) note the working\n  `broadcast=true`+empty-`to` fan-out with the 30-day/active/not-block_all recipient rule, or\n  (b) explicitly defer to the reconciled upstream doc — no plugin doc should repeat the stale\n  \"unsupported\" claim as fact.\n\n## Verification\nRe-run `am robot-docs guide` after any upstream fix lands to confirm the line is reconciled; a\nlive `send_message(broadcast=true, to=[])` in a multi-agent project fans out to the eligible\nset (read-only to confirm: check recipients on the resulting message).\n\n## Notes\nGrounded in the vault design note; citations are greppable file refs against\n~/projects/mcp_agent_mail_rust, not frozen line numbers.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-07-24T18:51:31.228165522Z","created_by":"gulp","updated_at":"2026-07-25T03:29:37.393364178Z","closed_at":"2026-07-25T03:29:37.393109725Z","close_reason":"Documented working broadcast=true+empty-to fan-out in driving-swarms-with-mail SKILL.md (commit aabfc8b); upstream am robot-docs guide 'intentionally unsupported' line confirmed stale — flagged for human to file against mcp_agent_mail_rust, not filed externally by agent.","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"tcp-p0x.8","depends_on_id":"tcp-p0x","type":"parent-child","created_at":"2026-07-24T18:51:31.228165522Z","created_by":"gulp","metadata":"{}","thread_id":""}]}
{"id":"tcp-p0x.9","title":"am robot atc + handoff: probe and document the two undocumented subcommands","description":"## Goal\nProbe and document the two `am robot` subcommands that have zero prose anywhere — `atc` and\n`handoff` — so the plugin's swarm/coordinator docs can reference them by behavior, not just by\none-line synopsis.\n\n## Background\nSurfaced by the `am robot` surface Rumsfeld-quadrant pass (vault design note\n`design/agent-mail-swarm-model-and-robot-surface.md`). Both subcommands **postdate the am\nrepo's own `docs/SPEC-script-migration-matrix.md`** and carry no \"when to use\" prose in any\nskill, spec, or vault note — only their `--help` flags:\n\n- **`robot atc`** — \"ATC snapshot: live server state with local DB fallback when unavailable.\"\n  Synopsis suggests an operator/liveness surface (the \"ATC\" = air-traffic-control metaphor).\n  Unknown: what it returns, how the local-DB fallback manifests, whether it's read-only.\n- **`robot handoff`** — \"Stale bead ownership handoff dashboard with dry-run reopen\n  recommendations\" (`--stale-minutes --active-minutes --fresh-comment-minutes`). This is the one\n  robot subcommand whose synopsis directly gestures at swarm hygiene (detecting abandoned worker\n  ownership) — directly relevant to the coordinator loop — yet is entirely undocumented.\n\n## Non-goals\n- Not an am source change; this is discovery + documentation.\n- Do not act on `handoff --dry-run` reopen recommendations against live beads until the\n  command's semantics are confirmed safe.\n\n## Acceptance Criteria\n- `atc` and `handoff` each get a short \"what it returns / when to use / read-only?\" note,\n  sourced from a live `--json` probe (and, where cheap, a source read of the am crate).\n- `handoff`'s relevance to the coordinator loop is captured where the swarm docs live\n  (e.g. `driving-swarms-with-mail` or a coordinator skill) — at minimum, that it exists and\n  surfaces stale bead ownership with a dry-run reopen preview.\n- The Rumsfeld note's \"known unknowns\" entry for atc/handoff is updated to \"known knowns\" once\n  characterized.\n\n## Verification\n`am robot atc --json` and `am robot handoff --json --dry-run` run cleanly and their output shape\nis recorded; a re-read of the design note reflects the promotion from unknown to characterized.\n\n## Finding (2026-07-24 probe) — `atc` partially characterized\nLive `am robot atc --json` on the installed 0.3.21 base build returns\n`{\"enabled\": false, \"source\": \"disabled\", \"note\": \"ATC is disabled by configuration.\"}` with\n`_actions: [\"Start the local server for live ATC state: am serve-http\"]`. Cause: the systemd unit\nsets `AM_ATC_ENABLED=false` (the ATC *adaptive-learning sidecar* — `atc.sqlite3` — is\nnon-functional in base-mode; schema never created). So on this machine `robot atc` is **not** a\ngeneral server-liveness probe — it reports the ATC subsystem's own (disabled) state. **Still\nopen:** what `robot atc` returns with ATC *enabled*, and `robot handoff` (fully unprobed).\n\nCorollary surfaced same probe: the reliable local-DB liveness read is **`robot status`** (returns\ninstantly even when the HTTP server is wedged and `am status` hangs) — captured in the wedge\ndiagnosis (beads `tcp-p0x.10` diagnosis skill, `tcp-p0x.11` systemd watchdog). Design note\n`design/agent-mail-swarm-model-and-robot-surface.md` updated: `atc` moved from known-unknown to\npartially-characterized.\n\n## Notes\nPairs with b-f8 (broadcast drift) — both are AM-surface findings from the same research pass,\nfiled so today's synthesis doesn't evaporate before it's actioned.","status":"closed","priority":3,"issue_type":"task","created_at":"2026-07-24T18:51:31.377958827Z","created_by":"gulp","updated_at":"2026-07-25T03:30:25.121312565Z","closed_at":"2026-07-25T03:30:25.121087838Z","close_reason":"Probed am robot atc --json and am robot handoff --json --dry-run against am 0.3.21; both documented in driving-swarms-with-mail SKILL.md 'Coordinator extras' section (commit e36fc89). handoff confirmed always-read-only per its own --help text. atc confirmed disabled-by-config on this install (AM_ATC_ENABLED=false), matching the earlier partial-characterization finding; ATC-enabled output shape remains unprobed (out of scope — no enabled instance available). Vault Rumsfeld-note known-unknowns update proposed to WindyBarn, not applied (HITL).","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"tcp-p0x.9","depends_on_id":"tcp-p0x","type":"parent-child","created_at":"2026-07-24T18:51:31.377958827Z","created_by":"gulp","metadata":"{}","thread_id":""}]}
{"id":"tcp-ssu","title":"monitor: project resolution silently falls through to Deno.cwd() → wrong bus; document --cwd + log resolved bus","description":"## Summary\n\nThe `monitor`/`watch` project resolution falls through to `Deno.cwd()` when\n`CLAUDE_PROJECT_DIR` is unset, so the watch can silently arm on the **wrong bus**.\nBecause the Agent Mail `project_key` IS the absolute cwd path, a wrong cwd watches\na different project's inbox with no error — and (pre-fix) an empty wrong bus\nsurfaced as a misleading \"poll FAILED\" rather than \"nothing here.\"\n\nField-reported by QuietHeron (msg #27704) while dogfooding plugin 0.4.0 / am\n0.3.21: `CLAUDE_PROJECT_DIR` was unset under the Monitor host, the chain fell\nthrough to `Deno.cwd()` = `/home/gulp/Vaults/everything-tooling`, while the\nidentity to watch was registered only on `/home/gulp/Vaults/everything`.\n\n## Background\n\n`src/cli.ts:67` (approx):\n`resolveProject = explicit ?? globalCwd ?? $CLAUDE_PROJECT_DIR ?? Deno.cwd()`\n\nThe `Deno.cwd()` tail is a silent-wrong-bus hazard under a Monitor host, whose\nshell cwd is arbitrary relative to the session's project. The operator has no\nvisibility into which bus was actually resolved.\n\n## Steps to Reproduce\n\n1. Register an identity on project A only (e.g. `/home/gulp/Vaults/everything`).\n2. Arm the monitor from a shell whose cwd is project B, with `CLAUDE_PROJECT_DIR`\n   unset and no `--cwd`/`--project` flag.\n3. The watch resolves to project B's bus (via `Deno.cwd()`), not A — silently\n   watching the wrong inbox. No error names the mismatch.\n\n## Acceptance Criteria\n\n- The toggle skill's \"arm with an identity\" section documents `--cwd <project>`\n  (and/or `--project`) alongside `AGENT_NAME=`, so an operator knows how to pin\n  the bus explicitly.\n- On the first successful poll, the watch logs the **resolved** project/bus path,\n  so the operator can confirm which inbox is being watched.\n- Consider: when project resolution falls through to `Deno.cwd()` (no explicit\n  flag, no `CLAUDE_PROJECT_DIR`), emit a one-line warning that the bus was\n  inferred from cwd and may be wrong — rather than resolving silently.\n\n## Verification\n\n- Arm with `--cwd`/`--project` and confirm the logged resolved bus matches.\n- Arm with everything unset from an unrelated cwd and confirm the warning fires\n  and the resolved bus is logged.\n- `deno test` green; docs reviewed.\n\n## Notes\n\nRelated but distinct from tcp-p0x.15 (show project-key in the notification\nlabel): .15 surfaces the bus in the steady-state notification; this bead is about\n**arm-time** resolution visibility + the explicit-pin doc gap + the silent\nwrong-cwd fallback. Overlaps the same `cli.ts` resolution surface as the FS-scope\nwork (tcp-p0x.16.2) — sequence after it to avoid a collision.","status":"closed","priority":2,"issue_type":"bug","created_at":"2026-07-25T03:37:17.361350107Z","created_by":"gulp","updated_at":"2026-07-25T07:56:14.932725135Z","closed_at":"2026-07-25T07:56:14.932512881Z","close_reason":"Provenance-aware project resolution: resolveProjectFrom returns {path, source}; noteResolvedBus confirms the resolved bus at arm time and WARNS on cwd-fallthrough (stdout for monitor, stderr for watch). Empty --cwd/env now falls through instead of resolving to ''. Toggle skill documents pinning via --cwd/CLAUDE_PROJECT_DIR. Chose arm-time logging over first-poll: visible even if the poll errors, which was the original 'poll FAILED' confusion. cli_test.ts covers the provenance table + warn-only-on-fallthrough.","source_repo":".","compaction_level":0,"original_size":0}
